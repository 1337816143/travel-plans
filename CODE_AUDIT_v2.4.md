# 青岛旅行计划 v2.4.0 全面代码、数据与功能审计

## 一、发布结论

v2.4.0 已完成本轮要求的全部项目：

1. 消除剩余约 29 KB 的 legacy 主体。
2. 拆分应用状态、坐标转换、底图控制、高德加载、高德助手控制器和页面启动。
3. Leaflet 与高德地图适配器改为显式上下文工厂，可独立实例化测试。
4. 天气、路况、地点搜索、周边搜索、定位和路线规划统一返回结果对象。
5. 重复路线点按出现次数分别记录状态，并迁移 v2.3 本机状态。
6. 体力与热风险加入可核验步行连接、步行折线高程、活动时段逐小时体感温度和降水概率。
7. 雨天备选按绕行距离、室内程度、预约要求和静态开放状态排序。
8. 日历、打印、风险计算和本机数据管理改为首次打开“旅行工具”后按需加载。
9. 建立首次包和总包 gzip 预算。
10. 保留 v1.0.15 作为不可变最终回退。

高德公开密钥、域名白名单、配额和服务端代理仍按用户要求延期，本版未声称完成。

---

## 二、legacy 消除结果

### v2.3.0

- `src-v2/app/legacy-app.js`：约 28,954 字节。

### v2.4.0

- `src-v2/app/legacy-app.js`：140 字节。
- 文件只保留版本兼容标识，不再承载应用逻辑。

迁出的模块：

### 应用与启动

- `src-v2/core/app-state.js`
- `src-v2/ui/app-bootstrap.js`
- `src-v2/ui/amap-assistant-controller.js`

### 地图基础设施

- `src-v2/map/coordinate-service.js`
- `src-v2/map/basemap-controller.js`
- `src-v2/map/amap-loader.js`

### 地图适配器

- `src-v2/map/leaflet-adapter.js`
- `src-v2/map/amap-adapter.js`
- `src-v2/map/map-adapters.js`
- `src-v2/map/render-model.js`

### 服务与按需工具

- `src-v2/services/service-result.js`
- `src-v2/services/service-facade.js`
- `src-v2/services/risk-metrics-service.js`
- `src-v2/ui/trip-tools-loader.js`

自动迁移报告：`MIGRATION_V2.4.json`。

### 主动修复

迁移后的 `app-state.js` 曾保留两个同名 `initFallback()`。浏览器会采用后声明，但维护时容易产生歧义。构建阶段现通过 Acorn AST 自动删除较早的重复顶层函数，只保留最后一个有效声明。

---

## 三、应用状态与显式上下文

`TravelAppContext` 现在集中提供：

- 应用状态：选中日期、路线显示、推荐模式、地图引擎、当前底图。
- Leaflet 对象：地图、聚合层、路线层、酒店层、标注映射。
- 高德对象：地图实例、标注、覆盖物、聚合器、信息窗、服务覆盖物、当前位置和路况状态。
- 配置：高德配置引用、旅行年份、缓存键和面板键。
- 数据访问：点位、日程、预约、`pointById()`、`routePoints()`。
- 本机存储和通用 UI 工具。

### 当前状态

状态已从 legacy 文件迁出，并通过统一上下文提供给模块。

### 仍保留的兼容层

为避免一次重写所有现有调用，`TravelAppContext` 的生产实例仍通过 getter / setter 连接部分历史词法变量。该结构已经把依赖边界显式化，但还不是完整 reducer 或不可变状态树。

### 后续优化方案

下一阶段可建立：

```js
createTravelStore({
  selectedDay,
  mapEngine,
  routeVisible,
  recommendationMode,
  basemap,
  bookingProgress
})
```

并使用事件或订阅机制替代跨模块直接赋值。应逐组迁移，避免一次性状态重写造成交互回归。

---

## 四、地图适配器独立化

新增工厂：

```js
createLeafletTravelAdapter(context)
createAmapTravelAdapter(context)
```

两个工厂均返回：

```js
{
  id,
  context,
  ready,
  resize,
  view,
  setView,
  renderMarkers,
  renderRoute,
  fitPoints,
  clearLayer
}
```

### Leaflet 上下文

显式传入：

- `map`
- `clusters`
- `routeLayer`
- `hotelLayer`
- `markers`
- 点位和日程访问器
- 标注、弹窗、方向箭头构建函数

### 高德上下文

显式传入：

- 高德地图实例
- 聚合器和覆盖物集合
- 坐标转换服务
- POI 坐标解析器
- 共享渲染模型
- 点位和日程访问器

### 独立测试

Playwright 中已使用伪上下文单独实例化 Leaflet 适配器，验证：

- 工厂不要求生产地图全局变量存在。
- 返回的适配器保留传入上下文。
- 未注入地图实例时 `ready()` 正确返回 `false`。

### 仍可继续完善

默认生产适配器在创建上下文时仍引用一些兼容函数，例如 `visiblePoints()` 和 `routePoints()`。下一阶段可把这些函数移动到纯数据 selector 模块，使适配器测试不再需要任何页面函数。

---

## 五、统一服务返回结构

天气、路况、地点搜索、周边搜索、定位和路线规划统一返回：

```js
{
  ok,
  data,
  error,
  source,
  cached,
  reportedAt
}
```

### 字段含义

- `ok`：本次操作是否成功。
- `data`：真实业务结果；失败时通常为 `null`。
- `error`：规范化后的错误名称和信息；成功时为 `null`。
- `source`：高德天气、高德路线、高德地点搜索等来源名称。
- `cached`：是否直接使用缓存结果。
- `reportedAt`：结果对应的时间或调用时间。

### 本轮主动修复

1. 搜索无关键词原来会显示错误，却返回成功 Promise；现改为失败结果。
2. 地点搜索和周边搜索原来只更新 DOM，不返回搜索结果；现将高德原始结果放入 `data`。
3. 天气原来只更新天气卡；现返回实况、预报、旅行日期天气映射和发布时间。
4. 路线控制器桥接曾在 IIFE 外错误引用内部变量；现只调用公开的 `TravelServiceFacade.invoke('route')`。
5. 服务诊断现在能够识别结果对象中的 `ok:false`，不再只依赖 Promise 是否 rejected。

### 当前边界

现有服务函数仍同时负责网络访问和 DOM 更新。统一结果已经完成，但“纯服务”和“控制器”尚未完全分离。

### 后续优化方案

拆成：

```text
services/amap-search-client.js
services/amap-route-client.js
services/amap-weather-client.js
controllers/amap-search-controller.js
controllers/amap-route-controller.js
```

Client 只返回数据，Controller 决定如何渲染和提示错误。

---

## 六、重复点位状态升级

v2.3 状态键：

```text
date + pointId
```

同一住宿点作为起点和终点时会共享状态。

v2.4 状态键：

```text
date + routeOccurrence + pointId
```

示例：

```text
08-10:0:hotel-zone
08-10:1:hotel-zone
```

现在第一次出现可以标记“已完成”，第二次出现可以独立标记“跳过”或“延后”。

新状态结构：

```js
{
  schemaVersion: 2,
  updatedAt,
  data
}
```

首次读取时会把 `trip-stop-status-v2.3` 迁移到 `trip-stop-status-v2.4`。

---

## 七、体力与热风险模型

### 1. 活动时段

从日程文本解析：

```text
09:30–12:30
14:15–17:20
```

并按旅行活动窗口读取逐小时体感温度和降水概率。

已增加回归测试，确保 `09:30–12:30` 的户外安排按 180 分钟计算。

### 2. 可核验步行连接

不再把所有相邻景点之间的移动都视为步行。

分类规则：

- 明确步行连接或短距离连接：调用高德步行路线。
- 明确地铁、网约车、观光车、轮渡、接驳或明显跨区距离：标记为交通转场，不计入步行。
- 交通方式不明确：标记为未映射，不计入精确步行量。

页面会明确显示有多少段交通转场或未明确段没有计入步行。

这样比把青岛市区到崂山、黄岛等跨区路段错误换算为步行更可靠。

### 3. 高德步行折线

对确认可步行的连接，读取高德步行路线中的：

- 距离，单位米。
- 时长，单位秒。
- `steps[].polyline` 路线折线。

高德步行接口文档：

- https://lbs.amap.com/api/webservice/guide/api/direction

### 4. 累计爬升

- 将高德 GCJ-02 步行折线近似转换为 WGS84。
- 沿实际步行折线约每 350 米采样。
- 调用 Open-Meteo Elevation API。
- 使用 Copernicus DEM 90m 高程数据计算正向高差累计值。
- 单次最多 100 个采样点。

官方资料：

- https://open-meteo.com/en/docs/elevation-api
- https://dataspace.copernicus.eu/

页面已加入 Open-Meteo 与 Copernicus DEM 可访问署名链接。

### 5. 逐小时体感温度

调用 Open-Meteo Forecast API 的：

- `apparent_temperature`
- `precipitation_probability`
- `weather_code`

时区设置为 `Asia/Shanghai`，仅读取该日实际活动时段。

官方资料：

- https://open-meteo.com/en/docs

### 6. 缓存和回退

- 风险数据在本机缓存 6 小时。
- 高德步行接口失败时，仅对确认可步行的连接使用坐标估算。
- 高程失败时显示“高程暂不可用”，不伪造爬升数值。
- 逐小时预报失败或尚未进入预报范围时显示不可用状态。

### 重要边界

- 90m DEM 不具备台阶级、栈道级或小坡级精度。
- 景区内部游览轨迹没有完整 GPX 时，不能精确计算全部内部步行。
- 风险模型仍结合户外时长、坡路文本和休息段进行启发式评分。
- 结果是行程舒适度提示，不是医疗结论。

---

## 八、雨天替代排序

候选项综合：

1. 与当天正式路线最近点的距离。
2. 室内程度。
3. 是否存在必须预约或建议预约。
4. 静态状态中是否包含关闭或暂停。
5. 绕行公里数。

输出包括：

- 排名。
- 绕行距离。
- 室内度 1–3。
- 预约项数量。
- 已有开放时间或状态说明。

只进行候选排序，不自动修改正式行程。

### 当前边界

开放时间仍来自静态文本，不是实时营业状态；距离目前是候选点到当天路线最近点的直线距离，而不是完整交通绕行时长。

### 下一阶段建议

为备选点建立结构化字段：

```js
{
  indoorLevel,
  openingHours,
  bookingRequired,
  expectedDuration,
  area,
  liveClosureSource
}
```

随后使用高德驾车或公交矩阵计算真实绕行成本。

---

## 九、按需加载与体积预算

### v2.3.0

- 首次 gzip：85,517 字节。

### v2.4.0

- 首次 HTML：286,048 字节。
- 首次 gzip：80,670 字节。
- 按需工具源码：41,147 字节。
- 按需工具 gzip：14,054 字节。
- 总 gzip：94,724 字节。
- 首次加载减少：4,847 字节，约 5.67%。

### 按需包内容

- 风险指标服务。
- 当天模式和站点状态。
- 日历导出。
- 简洁路线卡复制、下载和打印。
- 本机数据管理。

### 网络加载行为

- 初始 HTML 不包含旅行工具实现。
- 首次点击“旅行工具”时下载 `assets/v2.4.0/lazy-tools.js`。
- Service Worker 安装阶段不预下载按需包。
- 用户主动执行“准备离线访问”时，按需包才加入离线缓存。

### CI 预算

- 首次 gzip 上限：83,806 字节。
- 总 gzip 上限：107,517 字节。
- 当前预算检查：通过。

报告：`BUNDLE_BUDGET_v2.4.json`。

### 后续性能建议

1. 对高德助手中的低频面板继续代码分割。
2. 将 ICS、打印模板和数据管理拆为多个更小的动态模块。
3. 使用模块打包器进行 tree shaking 和压缩，但必须保留可重复构建和 v1.0.15 回退。
4. 为 CSS 增加未使用规则检测。
5. 为首次交互时间和工具打开时间增加性能基线，而不仅是 gzip 预算。

---

## 十、数据 Schema 与发布链路

v2.4.0 Schema 当前通过：

- 39 个点位。
- 8 天日程。
- 3 个酒店。
- 8 个预约事项。
- 24 个来源。
- 9 个推荐点。

检查内容：

- ID 唯一。
- 经纬度合法。
- 日期属于旅行范围。
- 路线和预约引用存在。
- 路线数组连续。
- URL 为 HTTP / HTTPS。
- 必填来源不为空。

错误和警告均为 0。

生产加载顺序：

```text
v2.4.0
↓ 失败
v1.0.15
```

---

## 十一、本轮主动查出并修复的问题

1. **定位服务 catch 自动补丁语法错误**  
   改为带花括号的错误处理并继续抛出，让统一结果层准确标记失败。

2. **简洁路线卡换行被生成器提前解释**  
   保留 `\n` 转义，确保生成的按需 JavaScript 合法。

3. **活动时段和户外分钟正则丢失反斜杠**  
   构建阶段修复，并增加 180 分钟回归断言。

4. **累计爬升最初沿景点直线采样**  
   改为读取高德步行 `polyline`，沿可核验步行折线采样。

5. **跨区转场被错误计入步行**  
   增加 walking / transfer / unmapped 分类，只统计可核验步行连接。

6. **交通转场提示被插入三次**  
   构建补丁从全局替换改为只替换第一次，并增加提示次数测试。

7. **服务结果 `data` 为空**  
   天气、搜索和周边搜索现在返回真实业务数据。

8. **空关键词被标记为成功**  
   输入校验失败现在返回 `ok:false`。

9. **路线控制器桥接跨 IIFE 访问内部变量**  
   改为通过公开 `TravelServiceFacade.invoke('route')` 调用。

10. **Service Worker 提前下载按需包**  
    改为首次点击工具或主动准备离线时下载。

11. **迁移后同名兼容函数重复**  
    使用 AST 自动保留最后一个有效声明。

12. **外部高程和天气缺少可访问署名**  
    风险卡加入 Open-Meteo 和 Copernicus DEM 链接。

---

## 十二、仍存在的问题与具体方案

### P1：状态仍有兼容全局变量

**问题：** `TravelAppContext` 已显式化状态边界，但生产 getter / setter 仍连接历史变量。

**方案：** 建立独立 store、selector 和 action，按地图状态、旅行状态和 UI 状态分域迁移。

### P1：服务仍同时操作 DOM

**问题：** 网络客户端和 UI 控制器尚未彻底分离。

**方案：** Client 返回纯结果，Controller 消费结果并更新页面；为 Client 增加无 DOM 单元测试。

### P1：高德失败后 JSONP 回退会重复请求

**问题：** CORS 请求失败后会转 JSONP，网络异常时可能增加一次等待。

**方案：** 记录失败类型；只对 CORS / 浏览器限制回退 JSONP，对鉴权、配额和参数错误直接失败。

### P2：本机数据版本化尚不完整

**问题：** 站点状态已有 `schemaVersion`，预算、提醒和部分偏好仍是旧结构。

**方案：** 所有本机数据统一为：

```js
{ schemaVersion, updatedAt, data }
```

并建立逐版本迁移表。

### P2：逐小时天气存在预报窗口

**问题：** 旅行日期未进入服务预报范围时，无法获得真实逐小时体感。

**方案：** 明确显示“尚未进入预报范围”；接近出行日期后自动刷新，不使用历史平均值冒充预报。

### P2：累计爬升仍是近似值

**问题：** 90m DEM 无法识别短台阶、室内楼梯、栈道细节和局部小坡。

**方案：** 支持导入现场 GPX、官方景区步道或用户实际轨迹，并继续标注数据精度。

### P2：景区内部步行量不完整

**问题：** 当前精确步行主要覆盖点位之间可核验连接，景区内部游览路径仍不完整。

**方案：** 为崂山、八大关、小麦岛等建立内部路线节点或 GPX；未建立前不把估算包装成精确结果。

### P2：雨天开放状态不是实时数据

**问题：** 排序使用静态开放文字，无法识别临时闭馆、台风停运或预约已满。

**方案：** 增加结构化开放时间、官方公告来源和临时关闭状态；动态状态必须展示来源和更新时间。

### P2：外部 API 可用性没有 SLA

**问题：** 高德和 Open-Meteo 受网络、CORS、配额和服务状态影响。

**方案：** 增加请求超时、指数退避、缓存年龄、失败原因分类和最近成功时间；不把外部服务失败阻断静态行程查看。

### P3：总下载体积较 v2.3.0 增加

**问题：** 首次包更小，但打开全部工具后总 gzip 为 94,724 字节，高于 v2.3.0 的 85,517 字节。

**方案：** 继续把风险、日历、打印和数据管理拆分；只有使用对应功能时才加载对应代码。

### P3：localStorage 扩展能力有限

**问题：** 若以后保存轨迹、支出和变更日志，localStorage 容量和事务能力不足。

**方案：** 小型偏好继续使用 localStorage，轨迹、支出、缓存和日志迁入 IndexedDB。

### P3：尚无真实设备实验室基线

**问题：** CI 使用 Chromium / WebKit 设备与尺寸仿真，不等同于小米 HyperOS Chrome 和实体 iPhone Safari。

**方案：** 在出发前用至少一台安卓和一台 iPhone 实机测试定位、日历导入、离线缓存、高德 App 唤起和打印分享。

---

## 十三、功能拓展建议

1. **交通段手动纠正**：允许用户将某段标记为步行、地铁、打车、轮渡或观光车。
2. **GPX 导入与实走轨迹**：用真实轨迹替换景区内部估算。
3. **预算与实际支出分离**：记录预算、实际支付、支付方式和同行人分摊。
4. **下一站导航卡**：当前位置到下一站的步行 / 公交 / 打车对比。
5. **预约与 ICS 双向状态**：预约完成后生成更新或取消事件的 ICS。
6. **本机状态导入导出**：同行人之间传递 JSON 状态文件，不依赖后端账户。
7. **行程操作日志**：记录完成、跳过、延后和恢复操作，支持撤销。
8. **结构化开放时间**：识别当前是否开放，而不是只展示静态文字。
9. **临时关闭提醒**：从景区官方公告发现台风、检修、闭馆或停航。
10. **无障碍设置**：字号、高对比度、减少动画、完整键盘操作和屏幕阅读器说明。
11. **隐私说明页**：解释定位、缓存、API 请求和本机数据的处理方式。
12. **真实设备健康检查**：页面内提供定位、存储、离线、地图和外部服务的一键诊断。

---

## 十四、明确延期事项

本轮仍不处理：

- 高德 Web API 密钥代理。
- 密钥轮换。
- 域名白名单自动配置。
- 配额和异常告警自动配置。

原因：用户明确要求暂不处理。仓库公开密钥风险仍然存在，不应在审计或发布说明中标记为已解决。

---

## 十五、发布门槛

合并前必须通过：

- v2.4 canonical build。
- legacy 文件小于 1 KB。
- 显式适配器上下文检查。
- 统一服务结果和业务数据测试。
- 重复点位状态测试。
- 活动时段、户外分钟、步行 / 转场分类测试。
- 高德折线与高程采样测试。
- 雨天排序测试。
- 首次和总 gzip 预算。
- 数据 Schema。
- Pages 载荷和 `v2.4.0 → v1.0.15` 回退。
- 桌面 Chromium 两种分辨率。
- 安卓旗舰 Chromium 横竖屏。
- iPhone WebKit 横竖屏。
- 页面初始化无未捕获错误。
- 移动端路线抽屉和侧边隐藏回归。
- v1.0.15 固定历史入口未被覆盖。
