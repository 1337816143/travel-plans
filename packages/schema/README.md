# @qingdao/schema

青岛 v3 的 Zod 运行时事实源、推导类型、错误路径和显式版本迁移。当前版本是 `schemaVersion: 1`；未登记的未来版本会拒绝解析。

Phase 1 已提供模型骨架、一个人工补足的现有点位纵向样本，以及全部 49 个 v2.5.4 运行时点位的只读迁移入口。全量迁移结果仍是 `review-required`，不表示这些内容已经完成 v3 联网核验或获准发布。
