===== SUMMARY =====
HTML length: 98728
Title: 青岛情侣旅行计划｜2026.08.09–08.16

===== RELEVANT IDS =====
panel
clearRoutes
searchInput
searchBtn
searchResults
panelEdgeToggle
basemapSelect
amapConfigBtn
basemapState
mapNotice
dayRouteCard
map
amapMap

===== RELEVANT DOM TAGS =====
<aside class="panel" id="panel" aria-label="旅行控制面板">
<div class="toolbar">
<button class="btn" id="clearRoutes">
<section class="section tab-panel active" data-panel="booking">
<section class="section tab-panel" data-panel="days">
<section class="section tab-panel" data-panel="search">
<div class="search-row">
<input id="searchInput" placeholder="如：海底世界、海军博物馆、轮渡、崂山" aria-label="搜索点位">
<button class="btn primary" id="searchBtn">
<div id="searchResults" class="search-results">
<section class="section tab-panel" data-panel="recommend">
<section class="section tab-panel" data-panel="stay">
<section class="section tab-panel" data-panel="sources">
<main class="map-wrap">
<button class="mobile-toggle" id="menuBtn" aria-controls="panel" aria-expanded="false">
<button class="panel-edge-toggle" id="panelEdgeToggle" aria-label="收起或展开行程面板" title="收起或展开行程面板">
<div class="basemap-control">
<select id="basemapSelect">
<button type="button" id="amapConfigBtn" title="配置高德Key">
<div class="map-notice" id="mapNotice">
<div id="dayRouteCard" class="day-route-card" aria-live="polite">
<div id="map" class="map" aria-label="青岛旅行地图">
<div id="amapMap" class="map amap-map" aria-label="高德地图" hidden>
<div class="booking-controls">
<div class="route-marker-head" style="background:${color}">
<div class="route-direction" style="background:${color};transform:rotate(${rotation}deg)">
<div class="day-route-head">
<div class="day-route-note">
<button class="day-route-close" type="button">
<div class="day-route-seq">
<div class="search-bookings">
<button class="btn notice-amap" data-id="${p.id}">
<div class="search-card">
<div class="search-card-head">
<div class="search-card-head-right">
<div class="search-tip">
<div class="search-actions">
<button class="btn primary search-select" data-id="${p.id}">
<button class="btn secondary search-taxi" data-id="${p.id}">
<button class="btn rec-amap" data-id="${id}">

===== EXTERNAL RESOURCES =====
<link id="leafletCss" rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY=" crossorigin="" onerror="this.onerror=null;this.href='https://cdn.jsdelivr.net/npm/leaflet@1.9.4/dist/leaflet.css'">
<link id="clusterCss" rel="stylesheet" href="https://unpkg.com/leaflet.markercluster@1.5.3/dist/MarkerCluster.css" onerror="this.onerror=null;this.href='https://cdn.jsdelivr.net/npm/leaflet.markercluster@1.5.3/dist/MarkerCluster.css'">
<link id="clusterDefaultCss" rel="stylesheet" href="https://unpkg.com/leaflet.markercluster@1.5.3/dist/MarkerCluster.Default.css" onerror="this.onerror=null;this.href='https://cdn.jsdelivr.net/npm/leaflet.markercluster@1.5.3/dist/MarkerCluster.Default.css'">
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo=" crossorigin="" onerror="this.onerror=null;this.src='https://cdn.jsdelivr.net/npm/leaflet@1.9.4/dist/leaflet.js'">
<script src="https://unpkg.com/leaflet.markercluster@1.5.3/dist/leaflet.markercluster.js" onerror="this.onerror=null;this.src='https://cdn.jsdelivr.net/npm/leaflet.markercluster@1.5.3/dist/leaflet.markercluster.js'">

===== RELEVANT CSS RULES =====
:root { --panel:432px;--ink:#172033;--muted:#64748b;--line:#e2e8f0;--paper:#fff;--soft:#f8fafc;--accent:#2563eb;--danger:#b42318;--warn:#a15c00;--ok:#18794e;--shadow:0 12px 36px rgba(15,23,42,.17) }
.app { height:100vh;display:grid;grid-template-columns:var(--panel) minmax(0,1fr);overflow:hidden }
.panel { height:100vh;background:var(--paper);border-right:1px solid var(--line);overflow:auto;z-index:1002;position:relative;scroll-behavior:smooth }
.map-wrap,.map { height:100vh;position:relative }
.panel-head { padding:22px 22px 17px;background:linear-gradient(145deg,#eff6ff 0%,#ecfeff 52%,#fff7ed 100%);border-bottom:1px solid var(--line) }
.panel-head h1 { font-size:24px;line-height:1.22;margin:7px 0 8px }
.mobile-toggle { display:none;position:absolute;left:12px;top:12px;z-index:1100;border:0;background:#fff;box-shadow:var(--shadow);border-radius:10px;padding:9px 11px;font-weight:800 }
:root { --panel:min(92vw,440px) }
.panel { position:absolute;left:0;top:0;transform:translateX(-105%);transition:transform .22s ease;box-shadow:var(--shadow);width:var(--panel);border-right:0 }
.panel.open { transform:translateX(0) }
.mobile-toggle { display:block }
.panel-head h1 { font-size:21px }
.tab-panel { display:none }
.tab-panel.active { display:block }
.panel-edge-toggle { position:absolute;z-index:1300;left:calc(var(--panel) - 1px);top:50%;transform:translate(-50%,-50%);width:28px;height:54px;border:1px solid #cbd5e1;border-radius:0 13px 13px 0;background:rgba(255,255,255,.96);box-shadow:0 4px 14px rgba(15,23,42,.18);color:#334155;font-size:22px;line-height:1;cursor:pointer }
.app.panel-collapsed { grid-template-columns:0 minmax(0,1fr) }
.app.panel-collapsed .panel { transform:translateX(-105%) }
.app.panel-collapsed .panel-edge-toggle { left:0;transform:translate(0,-50%);border-radius:0 13px 13px 0 }
.app.panel-collapsed .panel-edge-toggle { font-size:0 }
.app.panel-collapsed .panel-edge-toggle:after { content:'›';font-size:22px }
.panel-edge-toggle { position:fixed;left:0;top:50%;transform:translate(0,-50%);border-radius:0 13px 13px 0 }
.panel.open~.map-wrap .panel-edge-toggle { left:var(--panel);transform:translate(-100%,-50%);border-radius:13px 0 0 13px }
.app.panel-collapsed .panel-edge-toggle { left:0 }

===== SCRIPT CONTEXT: collapse =====
":"🛤","rec-taidong":"🍢","rec-haipo":"🥬","rec-thirdbeach":"🌃","rec-qianhaiyan":"🍚","rec-zhongpin":"🥟"}; const CATEGORY={"景点":{color:'#2563eb'},"酒店":{color:'#7c3aed'},"住宿区域":{color:'#8b5cf6'},"交通节点":{color:'#0891b2'},"服务":{color:'#d97706'},"行程节点":{color:'#475569'},"备选":{color:'#059669'},"推荐":{color:'#db2777'}}; let map,clusters,routeLayer,hotelLayer,selectedDay=null,routeVisible=true,tileErrors=0,recommendationMode=false,selectedPresetId=''; let activeBasemap='osm',activeTileLayer=null,basemapFailures={},autoFailoverLock=false,amapInstance=null,amapMarkers=[],amapOverlays=[],mapEngine='leaflet'; let presetOrder=[];let currentSearchIds=[]; const markers=new Map(); const STORAGE_KEY='qingdao-v107-booking-progress'; const ORDER_KEY='qingdao-v107-preset-order'; const BASEMAP_KEY='qingdao-v107-basemap'; const AMAP_CONFIG_KEY='qingdao-v107-amap-config'; const PANEL_KEY='qingdao-v107-panel-collapsed'; let bookingProgress=loadJSON(STORAGE_KEY,{}); function loadJSON(key,fallback){try{const v=JSON.parse(localStorage.getItem(key));return v&&typeof v==='object'?v:fallback}catch(e){return fallback}} function saveJSON(key,value){try{localStorage.setItem(key,JSON.stringify(value))}catch(e){}} function escapeHtml(s){return String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))} function outOfChina(lat,lng){return lng<72.004||lng>137.8347||lat<0.8293||lat>55.8271} function transformLat(x,y){let r=-100+2*x+3*y+.2*y*y+.1*x*y+.2*Math.sqrt(Math.abs(x));r+=(20*Math.sin(6*x*Math.PI)+20*Math.sin(2*x*Math.PI))*2/3;r+=(20*Math.sin(y*Math.PI)+40*Math.sin(y/3*Math.PI))*2/3;r+=(160*Math.sin(y/12*Math.PI)+320*Math.sin(y*Math.PI/30))*2/3;return r} function transformLng(x,y){let r=300+x+2*y+.1*x*x+.1*x*y+.1*Math.sqrt(Math.abs(x));r+=(20*Math.sin(6*x*Math.PI)+20*Math.sin(2*x*Math.PI))*2/3;r+=(20*Math.sin(x*Math.PI)+40*Math.sin(x/3*Math.PI))*2/3;r+=(150*Math.sin(x/12*Math.PI)+300*Math.sin(x/30*Math.PI))*2/3;return r} function wgs84ToGcj02(lat,lng){if(outOfChina(lat,lng))return[lat,lng];const a=6378245,ee=.00669342162296594323,dLat=transformLat(lng-105,lat-35),dLng=transformLng(lng-105,lat-35),rad=lat/180*Math.PI,magic=1-ee*Math.sin(rad)*Math.sin(rad),sqrt=Math.sqrt(magic);return[lat+(dLat*180)/((a*(1-ee))/(magic*sqrt)*Math.PI),lng+(dLng*180)/(a/sqrt*Math.cos(rad)*Math.PI)]} function pointById(id){return POINTS.find(p=>p.id===id)} function bookingById(id){return BOOKINGS.find(b=>b.id===id)} function routePoints(schedule){return schedule.route.map(pointById).filter(Boolean)} function shortName(name){return String(name).replace(/^推荐住宿核心区：/,'').replace(/地铁站$/,'站').replace(/海水浴场$/,'浴场').replace(/景区.*$/,'景区')} function pointIcon(id){return
assList.toggle('active',active);btn.setAttribute('aria-selected',String(active))});document.querySelectorAll('.tab-panel').forEach(panel=>panel.classList.toggle('active',panel.dataset.panel===name));if(name==='recommend'&&!silent){recommendationMode=true;selectedDay=null;routeVisible=false;rebuildMarkers(null);if(routeLayer)routeLayer.clearLayers();setDayRouteCard(null);if(mapEngine==='amap')syncAmapView();else fitPoints(RECOMMENDED.map(pointById).filter(Boolean),12)}else if(name!=='recommend'&&recommendationMode&&!silent){recommendationMode=false;routeVisible=true;rebuildMarkers(selectedDay);drawRoutes(selectedDay);if(mapEngine==='amap')syncAmapView();else if(!selectedDay)fitPoints(POINTS.filter(p=>p.category!=='推荐'&&p.category!=='行程节点'),11)}} function bindTabs(){document.querySelectorAll('.tab-btn').forEach(btn=>btn.onclick=()=>activateTab(btn.dataset.tab))} function setPanelCollapsed(collapsed){const app=document.querySelector('.app'),panel=document.getElementById('panel'),edge=document.getElementById('panelEdgeToggle');app.classList.toggle('panel-collapsed',collapsed);if(innerWidth<=800){panel.classList.toggle('open',!collapsed);document.getElementById('menuBtn').setAttribute('aria-expanded',String(!collapsed))}edge.setAttribute('aria-expanded',String(!collapsed));saveJSON(PANEL_KEY,{collapsed});setTimeout(()=>{if(map)map.invalidateSize();if(amapInstance)amapInstance.resize()},240)} function bindUI(){document.getElementById('showAll').onclick=showAll;document.getElementById('clearRoutes').onclick=clearRoutes;document.getElementById('fitHotels').onclick=()=>{recommendationMode=false;rebuildMarkers(null);if(mapEngine==='amap'&&amapInstance)amapInstance.setZoomAndCenter(14,[120.3778,36.0648]);else if(map)map.setView([36.0648,120.3778],14);activateTab('stay')};document.getElementById('searchBtn').onclick=searchPoints;document.getElementById('searchInput').oninput=searchPoints;document.getElementById('searchInput').onkeydown=e=>{if(e.key==='Enter')searchPoints()};document.getElementById('presetDestination').onchange=e=>selectPresetDestination(e.target.value,{promote:true,from:'dropdown'});document.getElementById('basemapSelect').onchange=e=>switchBasemap(e.target.value);document.getElementById('amapConfigBtn').onclick=()=>{if(promptAmapConfig()&&document.getElementById('basemapSelect').value==='amap')switchToAmap()};document.getElementById('menuBtn').onclick=()=>setPanelCollapsed(document.getElementById('panel').classList.contains('open'));document.getElementById('panelEdgeToggle').onclick=()=>setPanelCollapsed(!document.querySelector('.app').classList.contains('panel-collapsed'));document.addEventListener('change',e=>{if(e.target.matches('[data-booking
e',panel.dataset.panel===name));if(name==='recommend'&&!silent){recommendationMode=true;selectedDay=null;routeVisible=false;rebuildMarkers(null);if(routeLayer)routeLayer.clearLayers();setDayRouteCard(null);if(mapEngine==='amap')syncAmapView();else fitPoints(RECOMMENDED.map(pointById).filter(Boolean),12)}else if(name!=='recommend'&&recommendationMode&&!silent){recommendationMode=false;routeVisible=true;rebuildMarkers(selectedDay);drawRoutes(selectedDay);if(mapEngine==='amap')syncAmapView();else if(!selectedDay)fitPoints(POINTS.filter(p=>p.category!=='推荐'&&p.category!=='行程节点'),11)}} function bindTabs(){document.querySelectorAll('.tab-btn').forEach(btn=>btn.onclick=()=>activateTab(btn.dataset.tab))} function setPanelCollapsed(collapsed){const app=document.querySelector('.app'),panel=document.getElementById('panel'),edge=document.getElementById('panelEdgeToggle');app.classList.toggle('panel-collapsed',collapsed);if(innerWidth<=800){panel.classList.toggle('open',!collapsed);document.getElementById('menuBtn').setAttribute('aria-expanded',String(!collapsed))}edge.setAttribute('aria-expanded',String(!collapsed));saveJSON(PANEL_KEY,{collapsed});setTimeout(()=>{if(map)map.invalidateSize();if(amapInstance)amapInstance.resize()},240)} function bindUI(){document.getElementById('showAll').onclick=showAll;document.getElementById('clearRoutes').onclick=clearRoutes;document.getElementById('fitHotels').onclick=()=>{recommendationMode=false;rebuildMarkers(null);if(mapEngine==='amap'&&amapInstance)amapInstance.setZoomAndCenter(14,[120.3778,36.0648]);else if(map)map.setView([36.0648,120.3778],14);activateTab('stay')};document.getElementById('searchBtn').onclick=searchPoints;document.getElementById('searchInput').oninput=searchPoints;document.getElementById('searchInput').onkeydown=e=>{if(e.key==='Enter')searchPoints()};document.getElementById('presetDestination').onchange=e=>selectPresetDestination(e.target.value,{promote:true,from:'dropdown'});document.getElementById('basemapSelect').onchange=e=>switchBasemap(e.target.value);document.getElementById('amapConfigBtn').onclick=()=>{if(promptAmapConfig()&&document.getElementById('basemapSelect').value==='amap')switchToAmap()};document.getElementById('menuBtn').onclick=()=>setPanelCollapsed(document.getElementById('panel').classList.contains('open'));document.getElementById('panelEdgeToggle').onclick=()=>setPanelCollapsed(!document.querySelector('.app').classList.contains('panel-collapsed'));document.addEventListener('change',e=>{if(e.target.matches('[data-booking-progress]'))setBookingProgress(e.target.dataset.bookingProgress,e.target.value)});document.addEventListener('click',e=>{const b=e.target.closest('[data-booking-channe
taset.panel===name));if(name==='recommend'&&!silent){recommendationMode=true;selectedDay=null;routeVisible=false;rebuildMarkers(null);if(routeLayer)routeLayer.clearLayers();setDayRouteCard(null);if(mapEngine==='amap')syncAmapView();else fitPoints(RECOMMENDED.map(pointById).filter(Boolean),12)}else if(name!=='recommend'&&recommendationMode&&!silent){recommendationMode=false;routeVisible=true;rebuildMarkers(selectedDay);drawRoutes(selectedDay);if(mapEngine==='amap')syncAmapView();else if(!selectedDay)fitPoints(POINTS.filter(p=>p.category!=='推荐'&&p.category!=='行程节点'),11)}} function bindTabs(){document.querySelectorAll('.tab-btn').forEach(btn=>btn.onclick=()=>activateTab(btn.dataset.tab))} function setPanelCollapsed(collapsed){const app=document.querySelector('.app'),panel=document.getElementById('panel'),edge=document.getElementById('panelEdgeToggle');app.classList.toggle('panel-collapsed',collapsed);if(innerWidth<=800){panel.classList.toggle('open',!collapsed);document.getElementById('menuBtn').setAttribute('aria-expanded',String(!collapsed))}edge.setAttribute('aria-expanded',String(!collapsed));saveJSON(PANEL_KEY,{collapsed});setTimeout(()=>{if(map)map.invalidateSize();if(amapInstance)amapInstance.resize()},240)} function bindUI(){document.getElementById('showAll').onclick=showAll;document.getElementById('clearRoutes').onclick=clearRoutes;document.getElementById('fitHotels').onclick=()=>{recommendationMode=false;rebuildMarkers(null);if(mapEngine==='amap'&&amapInstance)amapInstance.setZoomAndCenter(14,[120.3778,36.0648]);else if(map)map.setView([36.0648,120.3778],14);activateTab('stay')};document.getElementById('searchBtn').onclick=searchPoints;document.getElementById('searchInput').oninput=searchPoints;document.getElementById('searchInput').onkeydown=e=>{if(e.key==='Enter')searchPoints()};document.getElementById('presetDestination').onchange=e=>selectPresetDestination(e.target.value,{promote:true,from:'dropdown'});document.getElementById('basemapSelect').onchange=e=>switchBasemap(e.target.value);document.getElementById('amapConfigBtn').onclick=()=>{if(promptAmapConfig()&&document.getElementById('basemapSelect').value==='amap')switchToAmap()};document.getElementById('menuBtn').onclick=()=>setPanelCollapsed(document.getElementById('panel').classList.contains('open'));document.getElementById('panelEdgeToggle').onclick=()=>setPanelCollapsed(!document.querySelector('.app').classList.contains('panel-collapsed'));document.addEventListener('change',e=>{if(e.target.matches('[data-booking-progress]'))setBookingProgress(e.target.dataset.bookingProgress,e.target.value)});document.addEventListener('click',e=>{const b=e.target.closest('[data-booking-channel]');if(b)o
ationMode=true;selectedDay=null;routeVisible=false;rebuildMarkers(null);if(routeLayer)routeLayer.clearLayers();setDayRouteCard(null);if(mapEngine==='amap')syncAmapView();else fitPoints(RECOMMENDED.map(pointById).filter(Boolean),12)}else if(name!=='recommend'&&recommendationMode&&!silent){recommendationMode=false;routeVisible=true;rebuildMarkers(selectedDay);drawRoutes(selectedDay);if(mapEngine==='amap')syncAmapView();else if(!selectedDay)fitPoints(POINTS.filter(p=>p.category!=='推荐'&&p.category!=='行程节点'),11)}} function bindTabs(){document.querySelectorAll('.tab-btn').forEach(btn=>btn.onclick=()=>activateTab(btn.dataset.tab))} function setPanelCollapsed(collapsed){const app=document.querySelector('.app'),panel=document.getElementById('panel'),edge=document.getElementById('panelEdgeToggle');app.classList.toggle('panel-collapsed',collapsed);if(innerWidth<=800){panel.classList.toggle('open',!collapsed);document.getElementById('menuBtn').setAttribute('aria-expanded',String(!collapsed))}edge.setAttribute('aria-expanded',String(!collapsed));saveJSON(PANEL_KEY,{collapsed});setTimeout(()=>{if(map)map.invalidateSize();if(amapInstance)amapInstance.resize()},240)} function bindUI(){document.getElementById('showAll').onclick=showAll;document.getElementById('clearRoutes').onclick=clearRoutes;document.getElementById('fitHotels').onclick=()=>{recommendationMode=false;rebuildMarkers(null);if(mapEngine==='amap'&&amapInstance)amapInstance.setZoomAndCenter(14,[120.3778,36.0648]);else if(map)map.setView([36.0648,120.3778],14);activateTab('stay')};document.getElementById('searchBtn').onclick=searchPoints;document.getElementById('searchInput').oninput=searchPoints;document.getElementById('searchInput').onkeydown=e=>{if(e.key==='Enter')searchPoints()};document.getElementById('presetDestination').onchange=e=>selectPresetDestination(e.target.value,{promote:true,from:'dropdown'});document.getElementById('basemapSelect').onchange=e=>switchBasemap(e.target.value);document.getElementById('amapConfigBtn').onclick=()=>{if(promptAmapConfig()&&document.getElementById('basemapSelect').value==='amap')switchToAmap()};document.getElementById('menuBtn').onclick=()=>setPanelCollapsed(document.getElementById('panel').classList.contains('open'));document.getElementById('panelEdgeToggle').onclick=()=>setPanelCollapsed(!document.querySelector('.app').classList.contains('panel-collapsed'));document.addEventListener('change',e=>{if(e.target.matches('[data-booking-progress]'))setBookingProgress(e.target.dataset.bookingProgress,e.target.value)});document.addEventListener('click',e=>{const b=e.target.closest('[data-booking-channel]');if(b)openBookingChannel(b.dataset.bookingChannel,Number(b.dataset.ch
er)routeLayer.clearLayers();setDayRouteCard(null);if(mapEngine==='amap')syncAmapView();else fitPoints(RECOMMENDED.map(pointById).filter(Boolean),12)}else if(name!=='recommend'&&recommendationMode&&!silent){recommendationMode=false;routeVisible=true;rebuildMarkers(selectedDay);drawRoutes(selectedDay);if(mapEngine==='amap')syncAmapView();else if(!selectedDay)fitPoints(POINTS.filter(p=>p.category!=='推荐'&&p.category!=='行程节点'),11)}} function bindTabs(){document.querySelectorAll('.tab-btn').forEach(btn=>btn.onclick=()=>activateTab(btn.dataset.tab))} function setPanelCollapsed(collapsed){const app=document.querySelector('.app'),panel=document.getElementById('panel'),edge=document.getElementById('panelEdgeToggle');app.classList.toggle('panel-collapsed',collapsed);if(innerWidth<=800){panel.classList.toggle('open',!collapsed);document.getElementById('menuBtn').setAttribute('aria-expanded',String(!collapsed))}edge.setAttribute('aria-expanded',String(!collapsed));saveJSON(PANEL_KEY,{collapsed});setTimeout(()=>{if(map)map.invalidateSize();if(amapInstance)amapInstance.resize()},240)} function bindUI(){document.getElementById('showAll').onclick=showAll;document.getElementById('clearRoutes').onclick=clearRoutes;document.getElementById('fitHotels').onclick=()=>{recommendationMode=false;rebuildMarkers(null);if(mapEngine==='amap'&&amapInstance)amapInstance.setZoomAndCenter(14,[120.3778,36.0648]);else if(map)map.setView([36.0648,120.3778],14);activateTab('stay')};document.getElementById('searchBtn').onclick=searchPoints;document.getElementById('searchInput').oninput=searchPoints;document.getElementById('searchInput').onkeydown=e=>{if(e.key==='Enter')searchPoints()};document.getElementById('presetDestination').onchange=e=>selectPresetDestination(e.target.value,{promote:true,from:'dropdown'});document.getElementById('basemapSelect').onchange=e=>switchBasemap(e.target.value);document.getElementById('amapConfigBtn').onclick=()=>{if(promptAmapConfig()&&document.getElementById('basemapSelect').value==='amap')switchToAmap()};document.getElementById('menuBtn').onclick=()=>setPanelCollapsed(document.getElementById('panel').classList.contains('open'));document.getElementById('panelEdgeToggle').onclick=()=>setPanelCollapsed(!document.querySelector('.app').classList.contains('panel-collapsed'));document.addEventListener('change',e=>{if(e.target.matches('[data-booking-progress]'))setBookingProgress(e.target.dataset.bookingProgress,e.target.value)});document.addEventListener('click',e=>{const b=e.target.closest('[data-booking-channel]');if(b)openBookingChannel(b.dataset.bookingChannel,Number(b.dataset.channelIndex))});document.addEventListener('keydown',e=>{if(e.key==='Escape')setPanel
apEngine==='amap')syncAmapView();else fitPoints(RECOMMENDED.map(pointById).filter(Boolean),12)}else if(name!=='recommend'&&recommendationMode&&!silent){recommendationMode=false;routeVisible=true;rebuildMarkers(selectedDay);drawRoutes(selectedDay);if(mapEngine==='amap')syncAmapView();else if(!selectedDay)fitPoints(POINTS.filter(p=>p.category!=='推荐'&&p.category!=='行程节点'),11)}} function bindTabs(){document.querySelectorAll('.tab-btn').forEach(btn=>btn.onclick=()=>activateTab(btn.dataset.tab))} function setPanelCollapsed(collapsed){const app=document.querySelector('.app'),panel=document.getElementById('panel'),edge=document.getElementById('panelEdgeToggle');app.classList.toggle('panel-collapsed',collapsed);if(innerWidth<=800){panel.classList.toggle('open',!collapsed);document.getElementById('menuBtn').setAttribute('aria-expanded',String(!collapsed))}edge.setAttribute('aria-expanded',String(!collapsed));saveJSON(PANEL_KEY,{collapsed});setTimeout(()=>{if(map)map.invalidateSize();if(amapInstance)amapInstance.resize()},240)} function bindUI(){document.getElementById('showAll').onclick=showAll;document.getElementById('clearRoutes').onclick=clearRoutes;document.getElementById('fitHotels').onclick=()=>{recommendationMode=false;rebuildMarkers(null);if(mapEngine==='amap'&&amapInstance)amapInstance.setZoomAndCenter(14,[120.3778,36.0648]);else if(map)map.setView([36.0648,120.3778],14);activateTab('stay')};document.getElementById('searchBtn').onclick=searchPoints;document.getElementById('searchInput').oninput=searchPoints;document.getElementById('searchInput').onkeydown=e=>{if(e.key==='Enter')searchPoints()};document.getElementById('presetDestination').onchange=e=>selectPresetDestination(e.target.value,{promote:true,from:'dropdown'});document.getElementById('basemapSelect').onchange=e=>switchBasemap(e.target.value);document.getElementById('amapConfigBtn').onclick=()=>{if(promptAmapConfig()&&document.getElementById('basemapSelect').value==='amap')switchToAmap()};document.getElementById('menuBtn').onclick=()=>setPanelCollapsed(document.getElementById('panel').classList.contains('open'));document.getElementById('panelEdgeToggle').onclick=()=>setPanelCollapsed(!document.querySelector('.app').classList.contains('panel-collapsed'));document.addEventListener('change',e=>{if(e.target.matches('[data-booking-progress]'))setBookingProgress(e.target.dataset.bookingProgress,e.target.value)});document.addEventListener('click',e=>{const b=e.target.closest('[data-booking-channel]');if(b)openBookingChannel(b.dataset.bookingChannel,Number(b.dataset.channelIndex))});document.addEventListener('keydown',e=>{if(e.key==='Escape')setPanelCollapsed(true)});bindTabs();const saved=loadJSON(PANE
;else fitPoints(RECOMMENDED.map(pointById).filter(Boolean),12)}else if(name!=='recommend'&&recommendationMode&&!silent){recommendationMode=false;routeVisible=true;rebuildMarkers(selectedDay);drawRoutes(selectedDay);if(mapEngine==='amap')syncAmapView();else if(!selectedDay)fitPoints(POINTS.filter(p=>p.category!=='推荐'&&p.category!=='行程节点'),11)}} function bindTabs(){document.querySelectorAll('.tab-btn').forEach(btn=>btn.onclick=()=>activateTab(btn.dataset.tab))} function setPanelCollapsed(collapsed){const app=document.querySelector('.app'),panel=document.getElementById('panel'),edge=document.getElementById('panelEdgeToggle');app.classList.toggle('panel-collapsed',collapsed);if(innerWidth<=800){panel.classList.toggle('open',!collapsed);document.getElementById('menuBtn').setAttribute('aria-expanded',String(!collapsed))}edge.setAttribute('aria-expanded',String(!collapsed));saveJSON(PANEL_KEY,{collapsed});setTimeout(()=>{if(map)map.invalidateSize();if(amapInstance)amapInstance.resize()},240)} function bindUI(){document.getElementById('showAll').onclick=showAll;document.getElementById('clearRoutes').onclick=clearRoutes;document.getElementById('fitHotels').onclick=()=>{recommendationMode=false;rebuildMarkers(null);if(mapEngine==='amap'&&amapInstance)amapInstance.setZoomAndCenter(14,[120.3778,36.0648]);else if(map)map.setView([36.0648,120.3778],14);activateTab('stay')};document.getElementById('searchBtn').onclick=searchPoints;document.getElementById('searchInput').oninput=searchPoints;document.getElementById('searchInput').onkeydown=e=>{if(e.key==='Enter')searchPoints()};document.getElementById('presetDestination').onchange=e=>selectPresetDestination(e.target.value,{promote:true,from:'dropdown'});document.getElementById('basemapSelect').onchange=e=>switchBasemap(e.target.value);document.getElementById('amapConfigBtn').onclick=()=>{if(promptAmapConfig()&&document.getElementById('basemapSelect').value==='amap')switchToAmap()};document.getElementById('menuBtn').onclick=()=>setPanelCollapsed(document.getElementById('panel').classList.contains('open'));document.getElementById('panelEdgeToggle').onclick=()=>setPanelCollapsed(!document.querySelector('.app').classList.contains('panel-collapsed'));document.addEventListener('change',e=>{if(e.target.matches('[data-booking-progress]'))setBookingProgress(e.target.dataset.bookingProgress,e.target.value)});document.addEventListener('click',e=>{const b=e.target.closest('[data-booking-channel]');if(b)openBookingChannel(b.dataset.bookingChannel,Number(b.dataset.channelIndex))});document.addEventListener('keydown',e=>{if(e.key==='Escape')setPanelCollapsed(true)});bindTabs();const saved=loadJSON(PANEL_KEY,{collapsed:innerWidth<=800

===== SCRIPT CONTEXT: createTileLayer =====
rtocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, 'carto-dark':{name:'CARTO 深色',url:'https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, opentopo:{name:'OpenTopoMap 地形',url:'https://{s}.tile.opentopomap.org/{z}/{x}/{y}.png',maxZoom:17,attribution:'Map data &copy; OpenStreetMap contributors, SRTM | Map style &copy; OpenTopoMap'}, hot:{name:'OSM 人道主义',url:'https://{s}.tile.openstreetmap.fr/hot/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors, Tiles style by HOT'} }; const FALLBACK_ORDER=['osm','carto-light','carto-voyager','hot','opentopo','carto-dark']; function setBasemapState(text){const el=document.getElementById('basemapState');if(el)el.textContent=text} function createTileLayer(id){const c=BASEMAPS[id];if(!c)return null;const options={maxZoom:Number.isFinite(c.maxZoom)?c.maxZoom:19,attribution:c.attribution||'',crossOrigin:true};const sd=c.subdomains;if((typeof sd==='string'&&sd.length)||(Array.isArray(sd)&&sd.length))options.subdomains=sd;const layer=L.tileLayer(c.url,options);let errors=0,first=Date.now();layer.on('tileload',()=>{errors=Math.max(0,errors-1);setBasemapState(`${c.name} · 正常`)});layer.on('tileerror',()=>{errors++;basemapFailures[id]=(basemapFailures[id]||0)+1;if(Date.now()-first<12000&&errors>=5)autoSwitchBasemap(id)});return layer} function autoSwitchBasemap(failedId){if(autoFailoverLock||mapEngine!=='leaflet')return;autoFailoverLock=true;const current=FALLBACK_ORDER.indexOf(failedId),candidates=[...FALLBACK_ORDER.slice(current+1),...FALLBACK_ORDER.slice(0,current)];const next=candidates.find(id=>(basemapFailures[id]||0)<10);if(next){showMapNotice(`${BASEMAPS[failedId].name}加载异常，已自动切换到${BASEMAPS[next].name}`);switchBasemap(next,{manual:false})}else{const cfg=loadJSON(AMAP_CONFIG_KEY,{});if(cfg.key&&cfg.securityJsCode){showMapNotice('常规底图均加载异常，尝试切换高德地图');switchBasemap('amap',{manual:false})}else showMapNotice('多个底图服务均加载失败；请检查网络，或配置高德地图Key。')}setTimeout(()=>autoFailoverLock=false,2500)} function switchLeafletBasemap(id){if(!map||!BASEMAPS[id])return;const config=BASEMAPS[id],layerMax=Number.isFinite(config.maxZoom)?config.maxZoom:19;map.setMaxZoom(layerMax);if(map.getZoom()>layerMax)map.setZoom(layerMax);if(activeTileLayer)map.removeLayer(activeTileLayer);activeBasemap=id;activeTileLayer=createTileLayer(id);activeTileLayer.addTo(map);mapEngine='leaflet';document.getElementById('map').classList.remove('leaflet-map-hidden');document.getElementById('amapMap').classList.remove('active');document.getElementById('amapM
aflet')return;autoFailoverLock=true;const current=FALLBACK_ORDER.indexOf(failedId),candidates=[...FALLBACK_ORDER.slice(current+1),...FALLBACK_ORDER.slice(0,current)];const next=candidates.find(id=>(basemapFailures[id]||0)<10);if(next){showMapNotice(`${BASEMAPS[failedId].name}加载异常，已自动切换到${BASEMAPS[next].name}`);switchBasemap(next,{manual:false})}else{const cfg=loadJSON(AMAP_CONFIG_KEY,{});if(cfg.key&&cfg.securityJsCode){showMapNotice('常规底图均加载异常，尝试切换高德地图');switchBasemap('amap',{manual:false})}else showMapNotice('多个底图服务均加载失败；请检查网络，或配置高德地图Key。')}setTimeout(()=>autoFailoverLock=false,2500)} function switchLeafletBasemap(id){if(!map||!BASEMAPS[id])return;const config=BASEMAPS[id],layerMax=Number.isFinite(config.maxZoom)?config.maxZoom:19;map.setMaxZoom(layerMax);if(map.getZoom()>layerMax)map.setZoom(layerMax);if(activeTileLayer)map.removeLayer(activeTileLayer);activeBasemap=id;activeTileLayer=createTileLayer(id);activeTileLayer.addTo(map);mapEngine='leaflet';document.getElementById('map').classList.remove('leaflet-map-hidden');document.getElementById('amapMap').classList.remove('active');document.getElementById('amapMap').hidden=true;saveJSON(BASEMAP_KEY,{id});document.getElementById('basemapSelect').value=id;setTimeout(()=>map.invalidateSize(),80)} function promptAmapConfig(){const current=loadJSON(AMAP_CONFIG_KEY,{}),key=prompt('请输入高德 Web端(JS API) Key：',current.key||'');if(key===null)return false;const securityJsCode=prompt('请输入高德 securityJsCode（生产环境建议改用服务端代理，不要长期明文保存）：',current.securityJsCode||'');if(securityJsCode===null)return false;saveJSON(AMAP_CONFIG_KEY,{key:key.trim(),securityJsCode:securityJsCode.trim()});return Boolean(key.trim()&&securityJsCode.trim())} function loadAmapApi(){return new Promise((resolve,reject)=>{if(window.AMap&&window.AMapLoader){resolve(window.AMap);return}const cfg=loadJSON(AMAP_CONFIG_KEY,{});if(!cfg.key||!cfg.securityJsCode){reject(new Error('未配置高德Key'));return}window._AMapSecurityConfig={securityJsCode:cfg.securityJsCode};const done=()=>window.AMapLoader.load({key:cfg.key,version:'2.0'}).then(resolve).catch(reject);if(window.AMapLoader){done();return}const script=document.createElement('script');script.src='https://webapi.amap.com/loader.js';script.onload=done;script.onerror=()=>reject(new Error('高德加载器下载失败'));document.head.appendChild(script)})} function clearAmapOverlays(){if(!amapInstance)return;[...amapMarkers,...amapOverlays].forEach(x=>{try{amapInstance.remove(x)}catch(e){}});amapMarkers=[];amapOverlays=[]} function amapPointPosition(p){const [lat,lng]=wgs84ToGcj02(p.lat,p.lng);return[lng,lat]} function renderAmapView(){if(!amapInstance||!window.AMap)return;clearAmapOverlays();const ps=visiblePoints(selectedDay);ps.fo

===== SCRIPT CONTEXT: basemaps =====
检通过：</b>${POINTS.length}个点位、${SCHEDULES.length}天日程、${BOOKINGS.length}项预约联动、${RECOMMENDED.length}个其他推荐；点位图标全部唯一，预约状态（含放弃）跨面板同步，多底图容灾已启用。`}} function showMapNotice(msg){const el=document.getElementById('mapNotice');el.textContent=msg;el.classList.add('show')} function initFallback(){document.getElementById('map').style.display='none';document.getElementById('legend').style.display='none';const f=document.getElementById('offlineFallback');f.style.display='block';document.getElementById('offlineList').innerHTML=[...REQUIRED,...OPTIONAL].map(pointById).filter(Boolean).map(p=>`<div><b>${pointIcon(p.id)} ${escapeHtml(p.name)}</b><br>${escapeHtml(p.time)} · ${escapeHtml(p.status)}</div>`).join('')} const BASEMAPS={ osm:{name:'OSM 标准',url:'https://tile.openstreetmap.org/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}, 'carto-light':{name:'CARTO 浅色',url:'https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, 'carto-voyager':{name:'CARTO 导航',url:'https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, 'carto-dark':{name:'CARTO 深色',url:'https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, opentopo:{name:'OpenTopoMap 地形',url:'https://{s}.tile.opentopomap.org/{z}/{x}/{y}.png',maxZoom:17,attribution:'Map data &copy; OpenStreetMap contributors, SRTM | Map style &copy; OpenTopoMap'}, hot:{name:'OSM 人道主义',url:'https://{s}.tile.openstreetmap.fr/hot/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors, Tiles style by HOT'} }; const FALLBACK_ORDER=['osm','carto-light','carto-voyager','hot','opentopo','carto-dark']; function setBasemapState(text){const el=document.getElementById('basemapState');if(el)el.textContent=text} function createTileLayer(id){const c=BASEMAPS[id];if(!c)return null;const options={maxZoom:Number.isFinite(c.maxZoom)?c.maxZoom:19,attribution:c.attribution||'',crossOrigin:true};const sd=c.subdomains;if((typeof sd==='string'&&sd.length)||(Array.isArray(sd)&&sd.length))options.subdomains=sd;const layer=L.tileLayer(c.url,options);let errors=0,first=Date.now();layer.on('tileload',()=>{errors=Math.max(0,errors-1);setBasemapState(`${c.name} · 正常`)});layer.on('tileerror',()=>{errors++;basemapFailures[id]=(basemapFailures[id]||0)+1;if(Date.now()-first<12000&&errors>=5)autoSwitchBasemap(id)});return layer} function autoSwitchBasemap(failedId){if(autoFailoverLock||mapEngine!=='leaflet')return;autoFailo
ById('mapNotice');el.textContent=msg;el.classList.add('show')} function initFallback(){document.getElementById('map').style.display='none';document.getElementById('legend').style.display='none';const f=document.getElementById('offlineFallback');f.style.display='block';document.getElementById('offlineList').innerHTML=[...REQUIRED,...OPTIONAL].map(pointById).filter(Boolean).map(p=>`<div><b>${pointIcon(p.id)} ${escapeHtml(p.name)}</b><br>${escapeHtml(p.time)} · ${escapeHtml(p.status)}</div>`).join('')} const BASEMAPS={ osm:{name:'OSM 标准',url:'https://tile.openstreetmap.org/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}, 'carto-light':{name:'CARTO 浅色',url:'https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, 'carto-voyager':{name:'CARTO 导航',url:'https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, 'carto-dark':{name:'CARTO 深色',url:'https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, opentopo:{name:'OpenTopoMap 地形',url:'https://{s}.tile.opentopomap.org/{z}/{x}/{y}.png',maxZoom:17,attribution:'Map data &copy; OpenStreetMap contributors, SRTM | Map style &copy; OpenTopoMap'}, hot:{name:'OSM 人道主义',url:'https://{s}.tile.openstreetmap.fr/hot/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors, Tiles style by HOT'} }; const FALLBACK_ORDER=['osm','carto-light','carto-voyager','hot','opentopo','carto-dark']; function setBasemapState(text){const el=document.getElementById('basemapState');if(el)el.textContent=text} function createTileLayer(id){const c=BASEMAPS[id];if(!c)return null;const options={maxZoom:Number.isFinite(c.maxZoom)?c.maxZoom:19,attribution:c.attribution||'',crossOrigin:true};const sd=c.subdomains;if((typeof sd==='string'&&sd.length)||(Array.isArray(sd)&&sd.length))options.subdomains=sd;const layer=L.tileLayer(c.url,options);let errors=0,first=Date.now();layer.on('tileload',()=>{errors=Math.max(0,errors-1);setBasemapState(`${c.name} · 正常`)});layer.on('tileerror',()=>{errors++;basemapFailures[id]=(basemapFailures[id]||0)+1;if(Date.now()-first<12000&&errors>=5)autoSwitchBasemap(id)});return layer} function autoSwitchBasemap(failedId){if(autoFailoverLock||mapEngine!=='leaflet')return;autoFailoverLock=true;const current=FALLBACK_ORDER.indexOf(failedId),candidates=[...FALLBACK_ORDER.slice(current+1),...FALLBACK_ORDER.slice(0,current)];const next=candidates.find(id=>(basemapFailures[id]|
document.getElementById('offlineFallback');f.style.display='block';document.getElementById('offlineList').innerHTML=[...REQUIRED,...OPTIONAL].map(pointById).filter(Boolean).map(p=>`<div><b>${pointIcon(p.id)} ${escapeHtml(p.name)}</b><br>${escapeHtml(p.time)} · ${escapeHtml(p.status)}</div>`).join('')} const BASEMAPS={ osm:{name:'OSM 标准',url:'https://tile.openstreetmap.org/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}, 'carto-light':{name:'CARTO 浅色',url:'https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, 'carto-voyager':{name:'CARTO 导航',url:'https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, 'carto-dark':{name:'CARTO 深色',url:'https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, opentopo:{name:'OpenTopoMap 地形',url:'https://{s}.tile.opentopomap.org/{z}/{x}/{y}.png',maxZoom:17,attribution:'Map data &copy; OpenStreetMap contributors, SRTM | Map style &copy; OpenTopoMap'}, hot:{name:'OSM 人道主义',url:'https://{s}.tile.openstreetmap.fr/hot/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors, Tiles style by HOT'} }; const FALLBACK_ORDER=['osm','carto-light','carto-voyager','hot','opentopo','carto-dark']; function setBasemapState(text){const el=document.getElementById('basemapState');if(el)el.textContent=text} function createTileLayer(id){const c=BASEMAPS[id];if(!c)return null;const options={maxZoom:Number.isFinite(c.maxZoom)?c.maxZoom:19,attribution:c.attribution||'',crossOrigin:true};const sd=c.subdomains;if((typeof sd==='string'&&sd.length)||(Array.isArray(sd)&&sd.length))options.subdomains=sd;const layer=L.tileLayer(c.url,options);let errors=0,first=Date.now();layer.on('tileload',()=>{errors=Math.max(0,errors-1);setBasemapState(`${c.name} · 正常`)});layer.on('tileerror',()=>{errors++;basemapFailures[id]=(basemapFailures[id]||0)+1;if(Date.now()-first<12000&&errors>=5)autoSwitchBasemap(id)});return layer} function autoSwitchBasemap(failedId){if(autoFailoverLock||mapEngine!=='leaflet')return;autoFailoverLock=true;const current=FALLBACK_ORDER.indexOf(failedId),candidates=[...FALLBACK_ORDER.slice(current+1),...FALLBACK_ORDER.slice(0,current)];const next=candidates.find(id=>(basemapFailures[id]||0)<10);if(next){showMapNotice(`${BASEMAPS[failedId].name}加载异常，已自动切换到${BASEMAPS[next].name}`);switchBasemap(next,{manual:false})}else{const cfg=loadJSON(AMAP_CONFIG_KEY,{});if(cfg.key&&cfg.securityJsCod

===== SCRIPT CONTEXT: tileerror =====
ata &copy; OpenStreetMap contributors, SRTM | Map style &copy; OpenTopoMap'}, hot:{name:'OSM 人道主义',url:'https://{s}.tile.openstreetmap.fr/hot/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors, Tiles style by HOT'} }; const FALLBACK_ORDER=['osm','carto-light','carto-voyager','hot','opentopo','carto-dark']; function setBasemapState(text){const el=document.getElementById('basemapState');if(el)el.textContent=text} function createTileLayer(id){const c=BASEMAPS[id];if(!c)return null;const options={maxZoom:Number.isFinite(c.maxZoom)?c.maxZoom:19,attribution:c.attribution||'',crossOrigin:true};const sd=c.subdomains;if((typeof sd==='string'&&sd.length)||(Array.isArray(sd)&&sd.length))options.subdomains=sd;const layer=L.tileLayer(c.url,options);let errors=0,first=Date.now();layer.on('tileload',()=>{errors=Math.max(0,errors-1);setBasemapState(`${c.name} · 正常`)});layer.on('tileerror',()=>{errors++;basemapFailures[id]=(basemapFailures[id]||0)+1;if(Date.now()-first<12000&&errors>=5)autoSwitchBasemap(id)});return layer} function autoSwitchBasemap(failedId){if(autoFailoverLock||mapEngine!=='leaflet')return;autoFailoverLock=true;const current=FALLBACK_ORDER.indexOf(failedId),candidates=[...FALLBACK_ORDER.slice(current+1),...FALLBACK_ORDER.slice(0,current)];const next=candidates.find(id=>(basemapFailures[id]||0)<10);if(next){showMapNotice(`${BASEMAPS[failedId].name}加载异常，已自动切换到${BASEMAPS[next].name}`);switchBasemap(next,{manual:false})}else{const cfg=loadJSON(AMAP_CONFIG_KEY,{});if(cfg.key&&cfg.securityJsCode){showMapNotice('常规底图均加载异常，尝试切换高德地图');switchBasemap('amap',{manual:false})}else showMapNotice('多个底图服务均加载失败；请检查网络，或配置高德地图Key。')}setTimeout(()=>autoFailoverLock=false,2500)} function switchLeafletBasemap(id){if(!map||!BASEMAPS[id])return;const config=BASEMAPS[id],layerMax=Number.isFinite(config.maxZoom)?config.maxZoom:19;map.setMaxZoom(layerMax);if(map.getZoom()>layerMax)map.setZoom(layerMax);if(activeTileLayer)map.removeLayer(activeTileLayer);activeBasemap=id;activeTileLayer=createTileLayer(id);activeTileLayer.addTo(map);mapEngine='leaflet';document.getElementById('map').classList.remove('leaflet-map-hidden');document.getElementById('amapMap').classList.remove('active');document.getElementById('amapMap').hidden=true;saveJSON(BASEMAP_KEY,{id});document.getElementById('basemapSelect').value=id;setTimeout(()=>map.invalidateSize(),80)} function promptAmapConfig(){const current=loadJSON(AMAP_CONFIG_KEY,{}),key=prompt('请输入高德 Web端(JS API) Key：',current.key||'');if(key===null)return false;const securityJsCode=prompt('请输入高德 securityJsCode（生产环境建议改用服务端代理，不要长期明文保存）：',current.securityJsCode||'');if(securityJsCode===null)return false;saveJSON(AMAP_CONFIG_K

===== SCRIPT CONTEXT: invalidateSize =====
k<=count;k++){const ratio=count===1?.5:k/(count+1),lat=a.lat+(b.lat-a.lat)*ratio,lng=a.lng+(b.lng-a.lng)*ratio;L.marker([lat,lng],{icon:directionIcon(d.color,bearingRotation(a,b)),interactive:false,zIndexOffset:500}).addTo(routeLayer)}}} function fitPoints(ps,maxZoom=13){if(!map||!ps||!ps.length)return;map.fitBounds(L.latLngBounds(ps.map(p=>[p.lat,p.lng])).pad(.25),{maxZoom})} function closePanelOnMobile(){if(innerWidth<=800)setPanelCollapsed(true)} function filterDay(day){const d=SCHEDULES.find(x=>x.date===day);if(!d)return;recommendationMode=false;routeVisible=true;rebuildMarkers(day);drawRoutes(day);if(mapEngine==='amap')syncAmapView();else fitPoints(routePoints(d),13);document.querySelectorAll('.day-card').forEach(el=>el.classList.toggle('active',el.dataset.day===day));activateTab('days',true);closePanelOnMobile();setTimeout(()=>{if(mapEngine==='amap')syncAmapView();else if(map){map.invalidateSize();fitPoints(routePoints(d),13)}},260)} function showAll(){recommendationMode=false;selectedDay=null;routeVisible=true;rebuildMarkers(null);drawRoutes(null);if(mapEngine==='amap')syncAmapView();else fitPoints(POINTS.filter(p=>p.category!=='行程节点'&&p.category!=='推荐'),11);document.querySelectorAll('.day-card').forEach(el=>el.classList.remove('active'))} function clearRoutes(){routeVisible=false;if(routeLayer)routeLayer.clearLayers();setDayRouteCard(null);syncAmapView()} function dayBookingsHtml(d){const items=bookingItemsForDay(d.date);return items.length?`<div class="day-bookings"><div class="day-bookings-title">预约 / 购票</div><div class="booking-list">${items.map(x=>bookingItemHtml(x)).join('')}</div></div>`:''} function renderDays(){const open=new Set([...document.querySelectorAll('.day-card[open]')].map(x=>x.dataset.day));document.getElementById('days').innerHTML=SCHEDULES.map((d,i)=>{const level=dayBookingLevel(d.date);return `<details class="day-card" data-day="${d.date}" ${open.has(d.date)||(!open.size&&i===1)?'open':''}><summary><span class="day-dot" style="background:${d.color}"></span><div class="day-summary-layout"><div><button class="day-title-button" type="button" data-day-focus="${d.date}">8月${Number(d.date.slice(3))}日｜${escapeHtml(d.title)}</button><div class="day-summary">${escapeHtml(d.summary)}</div></div></div><div class="day-summary-right">${dotHtml(level)}<span class="day-action">展开</span></div></summary><div class="day-body"><div class="timeline">${d.items.map(x=>`<div class="t">${escapeHtml(x[0])}</div><div>${escapeHtml(x[1])}</div>`).join('')}</div>${dayBookingsHtml(d)}<div class="planb"><b>Plan B：</b>${escapeHtml(d.planB)}</div></div></details>`}).join('');document.querySelectorAll('[data-day-focus]').forEach(b=>b.addEventListener('clic
se})}else{const cfg=loadJSON(AMAP_CONFIG_KEY,{});if(cfg.key&&cfg.securityJsCode){showMapNotice('常规底图均加载异常，尝试切换高德地图');switchBasemap('amap',{manual:false})}else showMapNotice('多个底图服务均加载失败；请检查网络，或配置高德地图Key。')}setTimeout(()=>autoFailoverLock=false,2500)} function switchLeafletBasemap(id){if(!map||!BASEMAPS[id])return;const config=BASEMAPS[id],layerMax=Number.isFinite(config.maxZoom)?config.maxZoom:19;map.setMaxZoom(layerMax);if(map.getZoom()>layerMax)map.setZoom(layerMax);if(activeTileLayer)map.removeLayer(activeTileLayer);activeBasemap=id;activeTileLayer=createTileLayer(id);activeTileLayer.addTo(map);mapEngine='leaflet';document.getElementById('map').classList.remove('leaflet-map-hidden');document.getElementById('amapMap').classList.remove('active');document.getElementById('amapMap').hidden=true;saveJSON(BASEMAP_KEY,{id});document.getElementById('basemapSelect').value=id;setTimeout(()=>map.invalidateSize(),80)} function promptAmapConfig(){const current=loadJSON(AMAP_CONFIG_KEY,{}),key=prompt('请输入高德 Web端(JS API) Key：',current.key||'');if(key===null)return false;const securityJsCode=prompt('请输入高德 securityJsCode（生产环境建议改用服务端代理，不要长期明文保存）：',current.securityJsCode||'');if(securityJsCode===null)return false;saveJSON(AMAP_CONFIG_KEY,{key:key.trim(),securityJsCode:securityJsCode.trim()});return Boolean(key.trim()&&securityJsCode.trim())} function loadAmapApi(){return new Promise((resolve,reject)=>{if(window.AMap&&window.AMapLoader){resolve(window.AMap);return}const cfg=loadJSON(AMAP_CONFIG_KEY,{});if(!cfg.key||!cfg.securityJsCode){reject(new Error('未配置高德Key'));return}window._AMapSecurityConfig={securityJsCode:cfg.securityJsCode};const done=()=>window.AMapLoader.load({key:cfg.key,version:'2.0'}).then(resolve).catch(reject);if(window.AMapLoader){done();return}const script=document.createElement('script');script.src='https://webapi.amap.com/loader.js';script.onload=done;script.onerror=()=>reject(new Error('高德加载器下载失败'));document.head.appendChild(script)})} function clearAmapOverlays(){if(!amapInstance)return;[...amapMarkers,...amapOverlays].forEach(x=>{try{amapInstance.remove(x)}catch(e){}});amapMarkers=[];amapOverlays=[]} function amapPointPosition(p){const [lat,lng]=wgs84ToGcj02(p.lat,p.lng);return[lng,lat]} function renderAmapView(){if(!amapInstance||!window.AMap)return;clearAmapOverlays();const ps=visiblePoints(selectedDay);ps.forEach(p=>{const marker=new AMap.Marker({position:amapPointPosition(p),title:p.name,label:{content:`${pointIcon(p.id)} ${shortName(p.name)}`,direction:'right'},offset:new AMap.Pixel(-10,-10)});marker.on('click',()=>amapInstance.setZoomAndCenter(16,amapPointPosition(p)));amapMarkers.push(marker)});if(routeVisible){const days=selectedDay?SCHED
Id).filter(Boolean),12)}else if(name!=='recommend'&&recommendationMode&&!silent){recommendationMode=false;routeVisible=true;rebuildMarkers(selectedDay);drawRoutes(selectedDay);if(mapEngine==='amap')syncAmapView();else if(!selectedDay)fitPoints(POINTS.filter(p=>p.category!=='推荐'&&p.category!=='行程节点'),11)}} function bindTabs(){document.querySelectorAll('.tab-btn').forEach(btn=>btn.onclick=()=>activateTab(btn.dataset.tab))} function setPanelCollapsed(collapsed){const app=document.querySelector('.app'),panel=document.getElementById('panel'),edge=document.getElementById('panelEdgeToggle');app.classList.toggle('panel-collapsed',collapsed);if(innerWidth<=800){panel.classList.toggle('open',!collapsed);document.getElementById('menuBtn').setAttribute('aria-expanded',String(!collapsed))}edge.setAttribute('aria-expanded',String(!collapsed));saveJSON(PANEL_KEY,{collapsed});setTimeout(()=>{if(map)map.invalidateSize();if(amapInstance)amapInstance.resize()},240)} function bindUI(){document.getElementById('showAll').onclick=showAll;document.getElementById('clearRoutes').onclick=clearRoutes;document.getElementById('fitHotels').onclick=()=>{recommendationMode=false;rebuildMarkers(null);if(mapEngine==='amap'&&amapInstance)amapInstance.setZoomAndCenter(14,[120.3778,36.0648]);else if(map)map.setView([36.0648,120.3778],14);activateTab('stay')};document.getElementById('searchBtn').onclick=searchPoints;document.getElementById('searchInput').oninput=searchPoints;document.getElementById('searchInput').onkeydown=e=>{if(e.key==='Enter')searchPoints()};document.getElementById('presetDestination').onchange=e=>selectPresetDestination(e.target.value,{promote:true,from:'dropdown'});document.getElementById('basemapSelect').onchange=e=>switchBasemap(e.target.value);document.getElementById('amapConfigBtn').onclick=()=>{if(promptAmapConfig()&&document.getElementById('basemapSelect').value==='amap')switchToAmap()};document.getElementById('menuBtn').onclick=()=>setPanelCollapsed(document.getElementById('panel').classList.contains('open'));document.getElementById('panelEdgeToggle').onclick=()=>setPanelCollapsed(!document.querySelector('.app').classList.contains('panel-collapsed'));document.addEventListener('change',e=>{if(e.target.matches('[data-booking-progress]'))setBookingProgress(e.target.dataset.bookingProgress,e.target.value)});document.addEventListener('click',e=>{const b=e.target.closest('[data-booking-channel]');if(b)openBookingChannel(b.dataset.bookingChannel,Number(b.dataset.channelIndex))});document.addEventListener('keydown',e=>{if(e.key==='Escape')setPanelCollapsed(true)});bindTabs();const saved=loadJSON(PANEL_KEY,{collapsed:innerWidth<=800});setPanelCollapsed(Boolean(saved.coll

===== SCRIPT CONTEXT: L.map =====
ment.getElementById('legend').innerHTML='<b>每日路线总览</b>'+SCHEDULES.slice(1,8).map(d=>`<div class="legend-row"><span class="sw" style="border-color:${d.color}"></span><span>8/${d.date.slice(3)} ${escapeHtml(d.title.split('＋')[0])}</span></div>`).join('')+'<div class="overview-line-note">点击某日加粗标题后显示方向、序号与图标</div>'} function presetPointIds(){return [...REQUIRED,...OPTIONAL]} function normalizePresetOrder(){const saved=loadJSON(ORDER_KEY,[]),ids=presetPointIds();presetOrder=[...saved.filter(id=>ids.includes(id)),...ids.filter(id=>!saved.includes(id))]} function renderPresetDestinations(){const select=document.getElementById('presetDestination');select.innerHTML=`<option value="">请选择目的地</option><optgroup label="必去目的地">${REQUIRED.map(id=>pointById(id)).filter(Boolean).map(p=>`<option value="${p.id}">${escapeHtml(shortName(p.name))}</option>`).join('')}</optgroup><optgroup label="室内备选">${OPTIONAL.map(id=>pointById(id)).filter(Boolean).map(p=>`<option value="${p.id}">${escapeHtml(shortName(p.name))}</option>`).join('')}</optgroup>`;select.value=selectedPresetId;renderPresetCards()} function pointBookingsSection(p){const items=bookingItemsForPoint(p.id);return items.length?`<div class="search-bookings"><div class="day-bookings-title">预约 / 购票</div>${items.map(x=>bookingItemHtml(x)).join('')}</div>`:''} function destinationNoticeHtml(p){return `<div class="destination-notice" aria-live="polite"><div class="destination-notice-title">注意事项</div><h3>${pointIcon(p.id)} ${escapeHtml(p.name)}</h3><div class="meta">${escapeHtml(p.category)} · ${escapeHtml(p.status)} · ${escapeHtml(p.time)}</div><p><b>目的地说明：</b>${escapeHtml(p.detail)}</p><p><b>交通：</b>${escapeHtml(p.transport)}</p><p><b>现场提醒：</b>${escapeHtml(p.tips)}</p>${pointBookingsSection(p)}<div class="notice-actions"><button class="btn primary notice-focus" data-id="${p.id}">地图定位</button><button class="btn notice-amap" data-id="${p.id}">打开高德App</button></div></div>`} function renderPresetCards(){const box=document.getElementById('presetCards');box.innerHTML=presetOrder.map(id=>{const p=pointById(id),level=pointBookingLevel(id),selected=id===selectedPresetId;return `<div class="preset-card-wrap ${selected?'selected':''}"><button type="button" class="preset-card ${selected?'active':''}" data-preset-id="${id}"><span class="preset-icon">${pointIcon(id)}</span><span class="preset-card-main"><span class="preset-card-name">${escapeHtml(shortName(p.name))}</span><span class="preset-card-sub">${escapeHtml(p.status)}</span></span>${dotHtml(level)}</button>${selected?destinationNoticeHtml(p):''}</div>`}).join('');box.querySelectorAll('[data-preset-id]').forEach(b=>b.onclick=()=>selectPresetDestination(b.dataset.presetId,{promote
lect').value='amap';saveJSON(BASEMAP_KEY,{id:'amap'});setBasemapState('高德地图 · 正常')}).catch(err=>{showMapNotice(`高德地图加载失败：${err.message}`);document.getElementById('basemapSelect').value=activeBasemap;switchLeafletBasemap(activeBasemap||'osm')})} function switchBasemap(id,{manual=true}={}){if(id==='amap'){switchToAmap();return}switchLeafletBasemap(id);if(manual)showMapNotice(`已切换到${BASEMAPS[id].name}`)} function initFallback(){document.getElementById('map').style.display='none';document.getElementById('legend').style.display='none';const f=document.getElementById('offlineFallback');f.style.display='block';document.getElementById('offlineList').innerHTML=[...REQUIRED,...OPTIONAL].map(pointById).filter(Boolean).map(p=>`<div><b>${escapeHtml(p.name)}</b><br>${escapeHtml(p.time)} · ${escapeHtml(p.status)}</div>`).join('')} function initMap(){if(typeof L==='undefined'){initFallback();return}map=L.map('map',{zoomControl:true,preferCanvas:true,minZoom:2,maxZoom:20}).setView([36.066,120.38],12);const saved=loadJSON(BASEMAP_KEY,{id:'osm'}).id||'osm',initialBasemap=saved==='amap'?'osm':(BASEMAPS[saved]?saved:'osm');switchLeafletBasemap(initialBasemap);clusters=markerGroup();routeLayer=L.layerGroup().addTo(map);hotelLayer=L.layerGroup().addTo(map);POINTS.forEach(p=>{const m=L.marker([p.lat,p.lng],{icon:iconFor(p),title:p.name}).bindPopup(popup(p),{maxWidth:370});markers.set(p.id,m)});rebuildMarkers(null);clusters.addTo(map);L.circle([36.0648,120.3778],{radius:850,color:'#7c3aed',weight:2,fillColor:'#a78bfa',fillOpacity:.14,dashArray:'5 6'}).bindTooltip('推荐住宿核心区：五四广场—浮山所').addTo(hotelLayer);drawRoutes(null);fitPoints(POINTS.filter(p=>p.category!=='行程节点'&&p.category!=='推荐'),11);if(saved==='amap')setTimeout(()=>switchToAmap(),150)} function activateTab(name,silent=false){document.querySelectorAll('.tab-btn').forEach(btn=>{const active=btn.dataset.tab===name;btn.classList.toggle('active',active);btn.setAttribute('aria-selected',String(active))});document.querySelectorAll('.tab-panel').forEach(panel=>panel.classList.toggle('active',panel.dataset.panel===name));if(name==='recommend'&&!silent){recommendationMode=true;selectedDay=null;routeVisible=false;rebuildMarkers(null);if(routeLayer)routeLayer.clearLayers();setDayRouteCard(null);if(mapEngine==='amap')syncAmapView();else fitPoints(RECOMMENDED.map(pointById).filter(Boolean),12)}else if(name!=='recommend'&&recommendationMode&&!silent){recommendationMode=false;routeVisible=true;rebuildMarkers(selectedDay);drawRoutes(selectedDay);if(mapEngine==='amap')syncAmapView();else if(!selectedDay)fitPoints(POINTS.filter(p=>p.category!=='推荐'&&p.category!=='行程节点'),11)}} function bindTabs(){document.querySelectorAll('.tab-btn').forEach(

===== SCRIPT CONTEXT: L.tileLayer =====
StreetMap contributors &copy; CARTO'}, opentopo:{name:'OpenTopoMap 地形',url:'https://{s}.tile.opentopomap.org/{z}/{x}/{y}.png',maxZoom:17,attribution:'Map data &copy; OpenStreetMap contributors, SRTM | Map style &copy; OpenTopoMap'}, hot:{name:'OSM 人道主义',url:'https://{s}.tile.openstreetmap.fr/hot/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors, Tiles style by HOT'} }; const FALLBACK_ORDER=['osm','carto-light','carto-voyager','hot','opentopo','carto-dark']; function setBasemapState(text){const el=document.getElementById('basemapState');if(el)el.textContent=text} function createTileLayer(id){const c=BASEMAPS[id];if(!c)return null;const options={maxZoom:Number.isFinite(c.maxZoom)?c.maxZoom:19,attribution:c.attribution||'',crossOrigin:true};const sd=c.subdomains;if((typeof sd==='string'&&sd.length)||(Array.isArray(sd)&&sd.length))options.subdomains=sd;const layer=L.tileLayer(c.url,options);let errors=0,first=Date.now();layer.on('tileload',()=>{errors=Math.max(0,errors-1);setBasemapState(`${c.name} · 正常`)});layer.on('tileerror',()=>{errors++;basemapFailures[id]=(basemapFailures[id]||0)+1;if(Date.now()-first<12000&&errors>=5)autoSwitchBasemap(id)});return layer} function autoSwitchBasemap(failedId){if(autoFailoverLock||mapEngine!=='leaflet')return;autoFailoverLock=true;const current=FALLBACK_ORDER.indexOf(failedId),candidates=[...FALLBACK_ORDER.slice(current+1),...FALLBACK_ORDER.slice(0,current)];const next=candidates.find(id=>(basemapFailures[id]||0)<10);if(next){showMapNotice(`${BASEMAPS[failedId].name}加载异常，已自动切换到${BASEMAPS[next].name}`);switchBasemap(next,{manual:false})}else{const cfg=loadJSON(AMAP_CONFIG_KEY,{});if(cfg.key&&cfg.securityJsCode){showMapNotice('常规底图均加载异常，尝试切换高德地图');switchBasemap('amap',{manual:false})}else showMapNotice('多个底图服务均加载失败；请检查网络，或配置高德地图Key。')}setTimeout(()=>autoFailoverLock=false,2500)} function switchLeafletBasemap(id){if(!map||!BASEMAPS[id])return;const config=BASEMAPS[id],layerMax=Number.isFinite(config.maxZoom)?config.maxZoom:19;map.setMaxZoom(layerMax);if(map.getZoom()>layerMax)map.setZoom(layerMax);if(activeTileLayer)map.removeLayer(activeTileLayer);activeBasemap=id;activeTileLayer=createTileLayer(id);activeTileLayer.addTo(map);mapEngine='leaflet';document.getElementById('map').classList.remove('leaflet-map-hidden');document.getElementById('amapMap').classList.remove('active');document.getElementById('amapMap').hidden=true;saveJSON(BASEMAP_KEY,{id});document.getElementById('basemapSelect').value=id;setTimeout(()=>map.invalidateSize(),80)} function promptAmapConfig(){const current=loadJSON(AMAP_CONFIG_KEY,{}),key=prompt('请输入高德 Web端(JS API) Key：',current.key||'');if(key===null)return false;const se

===== SCRIPT CONTEXT: subdomains =====
{BOOKINGS.length}项预约联动、${RECOMMENDED.length}个其他推荐；点位图标全部唯一，预约状态（含放弃）跨面板同步，多底图容灾已启用。`}} function showMapNotice(msg){const el=document.getElementById('mapNotice');el.textContent=msg;el.classList.add('show')} function initFallback(){document.getElementById('map').style.display='none';document.getElementById('legend').style.display='none';const f=document.getElementById('offlineFallback');f.style.display='block';document.getElementById('offlineList').innerHTML=[...REQUIRED,...OPTIONAL].map(pointById).filter(Boolean).map(p=>`<div><b>${pointIcon(p.id)} ${escapeHtml(p.name)}</b><br>${escapeHtml(p.time)} · ${escapeHtml(p.status)}</div>`).join('')} const BASEMAPS={ osm:{name:'OSM 标准',url:'https://tile.openstreetmap.org/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}, 'carto-light':{name:'CARTO 浅色',url:'https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, 'carto-voyager':{name:'CARTO 导航',url:'https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, 'carto-dark':{name:'CARTO 深色',url:'https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, opentopo:{name:'OpenTopoMap 地形',url:'https://{s}.tile.opentopomap.org/{z}/{x}/{y}.png',maxZoom:17,attribution:'Map data &copy; OpenStreetMap contributors, SRTM | Map style &copy; OpenTopoMap'}, hot:{name:'OSM 人道主义',url:'https://{s}.tile.openstreetmap.fr/hot/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors, Tiles style by HOT'} }; const FALLBACK_ORDER=['osm','carto-light','carto-voyager','hot','opentopo','carto-dark']; function setBasemapState(text){const el=document.getElementById('basemapState');if(el)el.textContent=text} function createTileLayer(id){const c=BASEMAPS[id];if(!c)return null;const options={maxZoom:Number.isFinite(c.maxZoom)?c.maxZoom:19,attribution:c.attribution||'',crossOrigin:true};const sd=c.subdomains;if((typeof sd==='string'&&sd.length)||(Array.isArray(sd)&&sd.length))options.subdomains=sd;const layer=L.tileLayer(c.url,options);let errors=0,first=Date.now();layer.on('tileload',()=>{errors=Math.max(0,errors-1);setBasemapState(`${c.name} · 正常`)});layer.on('tileerror',()=>{errors++;basemapFailures[id]=(basemapFailures[id]||0)+1;if(Date.now()-first<12000&&errors>=5)autoSwitchBasemap(id)});return layer} function autoSwitchBasemap(failedId){if(autoFailoverLock||mapEngine!=='leaflet')return;autoFailoverLock=true;const current=FALLBACK_ORDER.indexOf(fa
function initFallback(){document.getElementById('map').style.display='none';document.getElementById('legend').style.display='none';const f=document.getElementById('offlineFallback');f.style.display='block';document.getElementById('offlineList').innerHTML=[...REQUIRED,...OPTIONAL].map(pointById).filter(Boolean).map(p=>`<div><b>${pointIcon(p.id)} ${escapeHtml(p.name)}</b><br>${escapeHtml(p.time)} · ${escapeHtml(p.status)}</div>`).join('')} const BASEMAPS={ osm:{name:'OSM 标准',url:'https://tile.openstreetmap.org/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}, 'carto-light':{name:'CARTO 浅色',url:'https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, 'carto-voyager':{name:'CARTO 导航',url:'https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, 'carto-dark':{name:'CARTO 深色',url:'https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, opentopo:{name:'OpenTopoMap 地形',url:'https://{s}.tile.opentopomap.org/{z}/{x}/{y}.png',maxZoom:17,attribution:'Map data &copy; OpenStreetMap contributors, SRTM | Map style &copy; OpenTopoMap'}, hot:{name:'OSM 人道主义',url:'https://{s}.tile.openstreetmap.fr/hot/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors, Tiles style by HOT'} }; const FALLBACK_ORDER=['osm','carto-light','carto-voyager','hot','opentopo','carto-dark']; function setBasemapState(text){const el=document.getElementById('basemapState');if(el)el.textContent=text} function createTileLayer(id){const c=BASEMAPS[id];if(!c)return null;const options={maxZoom:Number.isFinite(c.maxZoom)?c.maxZoom:19,attribution:c.attribution||'',crossOrigin:true};const sd=c.subdomains;if((typeof sd==='string'&&sd.length)||(Array.isArray(sd)&&sd.length))options.subdomains=sd;const layer=L.tileLayer(c.url,options);let errors=0,first=Date.now();layer.on('tileload',()=>{errors=Math.max(0,errors-1);setBasemapState(`${c.name} · 正常`)});layer.on('tileerror',()=>{errors++;basemapFailures[id]=(basemapFailures[id]||0)+1;if(Date.now()-first<12000&&errors>=5)autoSwitchBasemap(id)});return layer} function autoSwitchBasemap(failedId){if(autoFailoverLock||mapEngine!=='leaflet')return;autoFailoverLock=true;const current=FALLBACK_ORDER.indexOf(failedId),candidates=[...FALLBACK_ORDER.slice(current+1),...FALLBACK_ORDER.slice(0,current)];const next=candidates.find(id=>(basemapFailures[id]||0)<10);if(next){showMapNotice(`${BASEMAPS[failedId].name}加载异常
display='block';document.getElementById('offlineList').innerHTML=[...REQUIRED,...OPTIONAL].map(pointById).filter(Boolean).map(p=>`<div><b>${pointIcon(p.id)} ${escapeHtml(p.name)}</b><br>${escapeHtml(p.time)} · ${escapeHtml(p.status)}</div>`).join('')} const BASEMAPS={ osm:{name:'OSM 标准',url:'https://tile.openstreetmap.org/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}, 'carto-light':{name:'CARTO 浅色',url:'https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, 'carto-voyager':{name:'CARTO 导航',url:'https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, 'carto-dark':{name:'CARTO 深色',url:'https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, opentopo:{name:'OpenTopoMap 地形',url:'https://{s}.tile.opentopomap.org/{z}/{x}/{y}.png',maxZoom:17,attribution:'Map data &copy; OpenStreetMap contributors, SRTM | Map style &copy; OpenTopoMap'}, hot:{name:'OSM 人道主义',url:'https://{s}.tile.openstreetmap.fr/hot/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors, Tiles style by HOT'} }; const FALLBACK_ORDER=['osm','carto-light','carto-voyager','hot','opentopo','carto-dark']; function setBasemapState(text){const el=document.getElementById('basemapState');if(el)el.textContent=text} function createTileLayer(id){const c=BASEMAPS[id];if(!c)return null;const options={maxZoom:Number.isFinite(c.maxZoom)?c.maxZoom:19,attribution:c.attribution||'',crossOrigin:true};const sd=c.subdomains;if((typeof sd==='string'&&sd.length)||(Array.isArray(sd)&&sd.length))options.subdomains=sd;const layer=L.tileLayer(c.url,options);let errors=0,first=Date.now();layer.on('tileload',()=>{errors=Math.max(0,errors-1);setBasemapState(`${c.name} · 正常`)});layer.on('tileerror',()=>{errors++;basemapFailures[id]=(basemapFailures[id]||0)+1;if(Date.now()-first<12000&&errors>=5)autoSwitchBasemap(id)});return layer} function autoSwitchBasemap(failedId){if(autoFailoverLock||mapEngine!=='leaflet')return;autoFailoverLock=true;const current=FALLBACK_ORDER.indexOf(failedId),candidates=[...FALLBACK_ORDER.slice(current+1),...FALLBACK_ORDER.slice(0,current)];const next=candidates.find(id=>(basemapFailures[id]||0)<10);if(next){showMapNotice(`${BASEMAPS[failedId].name}加载异常，已自动切换到${BASEMAPS[next].name}`);switchBasemap(next,{manual:false})}else{const cfg=loadJSON(AMAP_CONFIG_KEY,{});if(cfg.key&&cfg.securityJsCode){showMapNotice('常规底图均加载异常，尝试切换高德地图');switchBasema
tps://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png',subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, opentopo:{name:'OpenTopoMap 地形',url:'https://{s}.tile.opentopomap.org/{z}/{x}/{y}.png',maxZoom:17,attribution:'Map data &copy; OpenStreetMap contributors, SRTM | Map style &copy; OpenTopoMap'}, hot:{name:'OSM 人道主义',url:'https://{s}.tile.openstreetmap.fr/hot/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors, Tiles style by HOT'} }; const FALLBACK_ORDER=['osm','carto-light','carto-voyager','hot','opentopo','carto-dark']; function setBasemapState(text){const el=document.getElementById('basemapState');if(el)el.textContent=text} function createTileLayer(id){const c=BASEMAPS[id];if(!c)return null;const options={maxZoom:Number.isFinite(c.maxZoom)?c.maxZoom:19,attribution:c.attribution||'',crossOrigin:true};const sd=c.subdomains;if((typeof sd==='string'&&sd.length)||(Array.isArray(sd)&&sd.length))options.subdomains=sd;const layer=L.tileLayer(c.url,options);let errors=0,first=Date.now();layer.on('tileload',()=>{errors=Math.max(0,errors-1);setBasemapState(`${c.name} · 正常`)});layer.on('tileerror',()=>{errors++;basemapFailures[id]=(basemapFailures[id]||0)+1;if(Date.now()-first<12000&&errors>=5)autoSwitchBasemap(id)});return layer} function autoSwitchBasemap(failedId){if(autoFailoverLock||mapEngine!=='leaflet')return;autoFailoverLock=true;const current=FALLBACK_ORDER.indexOf(failedId),candidates=[...FALLBACK_ORDER.slice(current+1),...FALLBACK_ORDER.slice(0,current)];const next=candidates.find(id=>(basemapFailures[id]||0)<10);if(next){showMapNotice(`${BASEMAPS[failedId].name}加载异常，已自动切换到${BASEMAPS[next].name}`);switchBasemap(next,{manual:false})}else{const cfg=loadJSON(AMAP_CONFIG_KEY,{});if(cfg.key&&cfg.securityJsCode){showMapNotice('常规底图均加载异常，尝试切换高德地图');switchBasemap('amap',{manual:false})}else showMapNotice('多个底图服务均加载失败；请检查网络，或配置高德地图Key。')}setTimeout(()=>autoFailoverLock=false,2500)} function switchLeafletBasemap(id){if(!map||!BASEMAPS[id])return;const config=BASEMAPS[id],layerMax=Number.isFinite(config.maxZoom)?config.maxZoom:19;map.setMaxZoom(layerMax);if(map.getZoom()>layerMax)map.setZoom(layerMax);if(activeTileLayer)map.removeLayer(activeTileLayer);activeBasemap=id;activeTileLayer=createTileLayer(id);activeTileLayer.addTo(map);mapEngine='leaflet';document.getElementById('map').classList.remove('leaflet-map-hidden');document.getElementById('amapMap').classList.remove('active');document.getElementById('amapMap').hidden=true;saveJSON(BASEMAP_KEY,{id});document.getElementById('basemapSelect').value=id;setTimeout(()=>map.invalidateSize(),80)} function promptAmapConfig(){const current=load
0,attribution:'&copy; OpenStreetMap contributors &copy; CARTO'}, opentopo:{name:'OpenTopoMap 地形',url:'https://{s}.tile.opentopomap.org/{z}/{x}/{y}.png',maxZoom:17,attribution:'Map data &copy; OpenStreetMap contributors, SRTM | Map style &copy; OpenTopoMap'}, hot:{name:'OSM 人道主义',url:'https://{s}.tile.openstreetmap.fr/hot/{z}/{x}/{y}.png',maxZoom:19,attribution:'&copy; OpenStreetMap contributors, Tiles style by HOT'} }; const FALLBACK_ORDER=['osm','carto-light','carto-voyager','hot','opentopo','carto-dark']; function setBasemapState(text){const el=document.getElementById('basemapState');if(el)el.textContent=text} function createTileLayer(id){const c=BASEMAPS[id];if(!c)return null;const options={maxZoom:Number.isFinite(c.maxZoom)?c.maxZoom:19,attribution:c.attribution||'',crossOrigin:true};const sd=c.subdomains;if((typeof sd==='string'&&sd.length)||(Array.isArray(sd)&&sd.length))options.subdomains=sd;const layer=L.tileLayer(c.url,options);let errors=0,first=Date.now();layer.on('tileload',()=>{errors=Math.max(0,errors-1);setBasemapState(`${c.name} · 正常`)});layer.on('tileerror',()=>{errors++;basemapFailures[id]=(basemapFailures[id]||0)+1;if(Date.now()-first<12000&&errors>=5)autoSwitchBasemap(id)});return layer} function autoSwitchBasemap(failedId){if(autoFailoverLock||mapEngine!=='leaflet')return;autoFailoverLock=true;const current=FALLBACK_ORDER.indexOf(failedId),candidates=[...FALLBACK_ORDER.slice(current+1),...FALLBACK_ORDER.slice(0,current)];const next=candidates.find(id=>(basemapFailures[id]||0)<10);if(next){showMapNotice(`${BASEMAPS[failedId].name}加载异常，已自动切换到${BASEMAPS[next].name}`);switchBasemap(next,{manual:false})}else{const cfg=loadJSON(AMAP_CONFIG_KEY,{});if(cfg.key&&cfg.securityJsCode){showMapNotice('常规底图均加载异常，尝试切换高德地图');switchBasemap('amap',{manual:false})}else showMapNotice('多个底图服务均加载失败；请检查网络，或配置高德地图Key。')}setTimeout(()=>autoFailoverLock=false,2500)} function switchLeafletBasemap(id){if(!map||!BASEMAPS[id])return;const config=BASEMAPS[id],layerMax=Number.isFinite(config.maxZoom)?config.maxZoom:19;map.setMaxZoom(layerMax);if(map.getZoom()>layerMax)map.setZoom(layerMax);if(activeTileLayer)map.removeLayer(activeTileLayer);activeBasemap=id;activeTileLayer=createTileLayer(id);activeTileLayer.addTo(map);mapEngine='leaflet';document.getElementById('map').classList.remove('leaflet-map-hidden');document.getElementById('amapMap').classList.remove('active');document.getElementById('amapMap').hidden=true;saveJSON(BASEMAP_KEY,{id});document.getElementById('basemapSelect').value=id;setTimeout(()=>map.invalidateSize(),80)} function promptAmapConfig(){const current=loadJSON(AMAP_CONFIG_KEY,{}),key=prompt('请输入高德 Web端(JS API) Key：',current.key||'');if(key===

===== RELEVANT FUNCTION NAMES =====
pointById
routePoints
pointIcon
bookingItemsForPoint
pointBookingLevel
amapLinks
launchAmap
visiblePoints
routeOrderMap
routeMarkerIcon
setDayRouteCard
drawRoutes
fitPoints
closePanelOnMobile
clearRoutes
presetPointIds
pointBookingsSection
renderSearchResults
searchPoints
focusPoint
showMapNotice
setBasemapState
createTileLayer
autoSwitchBasemap
switchLeafletBasemap
promptAmapConfig
loadAmapApi
clearAmapOverlays
amapPointPosition
renderAmapView
syncAmapView
switchToAmap
switchBasemap
initMap
setPanelCollapsed
