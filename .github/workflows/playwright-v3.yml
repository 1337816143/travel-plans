name: Playwright Qingdao v3 complete guide, rain and planner

on:
  pull_request:
    paths:
      - '.github/workflows/playwright-v3.yml'
      - 'apps/web/**'
      - 'data/qingdao/**'
      - 'packages/domain/**'
      - 'packages/map-core/**'
      - 'packages/planner/**'
      - 'packages/schema/**'
      - 'packages/storage/**'
      - 'src-v2.5.5/**'
      - 'scripts/build-v2.5.5.mjs'
      - 'scripts/validate-v2.5.5.mjs'
      - 'scripts/check-v2.5.4-freeze.mjs'
      - 'scripts/check-v3-v2-parity.mjs'
      - 'scripts/package-v3-pages.mjs'
      - 'scripts/serve-pages-preview.mjs'
      - 'scripts/validate-v3-pages.mjs'
      - 'tests-v3/**'
      - 'v3/**'
      - 'playwright.v3.config.js'
      - 'package-lock.json'
      - 'package.json'
  push:
    branches:
      - main
      - 'agent/qingdao-v3-*'
      - agent/v2.5.5-v3-rain-beach-contingency
    paths:
      - '.github/workflows/playwright-v3.yml'
      - 'apps/web/**'
      - 'data/qingdao/**'
      - 'packages/domain/**'
      - 'packages/map-core/**'
      - 'packages/planner/**'
      - 'packages/schema/**'
      - 'packages/storage/**'
      - 'src-v2.5.5/**'
      - 'scripts/build-v2.5.5.mjs'
      - 'scripts/validate-v2.5.5.mjs'
      - 'scripts/check-v2.5.4-freeze.mjs'
      - 'scripts/check-v3-v2-parity.mjs'
      - 'scripts/package-v3-pages.mjs'
      - 'scripts/serve-pages-preview.mjs'
      - 'scripts/validate-v3-pages.mjs'
      - 'tests-v3/**'
      - 'v3/**'
      - 'playwright.v3.config.js'
      - 'package-lock.json'
      - 'package.json'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  complete-guide-rain-planner-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - name: Install locked dependencies
        run: npm ci --ignore-scripts
      - name: Build current root and v3 preview
        run: |
          node scripts/build-v2.5.5.mjs
          node scripts/validate-v2.5.5.mjs
          npm run build:pages:v3
          npm run check:bundle:web:v3
          npm run validate:pages:v3
          git diff --exit-code -- index.html service-worker.js src/v2.5.5.html assets/v2.5.5 v3
      - name: Install Chromium and system dependencies
        run: npx playwright install --with-deps chromium
      - name: Run desktop and phone closed-loop tests
        run: npm run test:e2e:v3
      - name: Verify test workflow did not change source
        run: test -z "$(git status --porcelain)"
      - name: Upload screenshots and diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qingdao-v3-rain-contingency-planner-preview
          path: |
            artifacts/v3-web
            playwright-report-v3
            test-results-v3
            v3/release-manifest.json
            v3/rain-guide.json
          if-no-files-found: ignore
          retention-days: 14
