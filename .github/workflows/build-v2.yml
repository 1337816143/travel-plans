name: Build v2 modular release

on:
  push:
    branches:
      - agent/v2.5.1-wishlist-completeness
    paths:
      - 'src-v2/**'
      - 'scripts/validate-data-schema.mjs'
      - 'scripts/build-v2.mjs'
      - 'scripts/validate-v2.mjs'
      - 'scripts/inspect-v2.5.mjs'
      - 'tests/**'
      - 'package.json'
      - '.github/workflows/build-v2.yml'
  pull_request:
    paths:
      - 'src-v2/**'
      - 'scripts/validate-data-schema.mjs'
      - 'scripts/build-v2.mjs'
      - 'scripts/validate-v2.mjs'
      - 'scripts/inspect-v2.5.mjs'
      - 'tests/**'
      - 'package.json'
      - '.github/workflows/build-v2.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: build-v2.5.1-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Install deterministic build dependencies
        run: npm install --ignore-scripts
      - name: Build girlfriend wishlist completeness release v2.5.1
        run: node scripts/build-v2.mjs
      - name: Verify generated release is committed
        run: |
          TARGETS="src-v2/data/generated/schema-report.js src/v2.5.1.html assets/v2.5.1 index.html versions service-worker.js PAGES_SETUP.md DEPLOYMENT.md MIGRATION_V2.5.1.json DATA_SCHEMA_REPORT_v2.5.1.json BUNDLE_BUDGET_v2.5.1.json"
          if ! git diff --quiet -- $TARGETS; then
            echo "::error::Generated v2.5.1 release differs from committed artifacts"
            git diff --stat -- $TARGETS
            git diff --name-only -- $TARGETS
            exit 1
          fi
          echo "v2.5.1 build is reproducible and committed artifacts are current"
