name: Validate v2 modular release

on:
  push:
    branches:
      - 'agent/v2.5.5-*'
  pull_request:
    paths:
      - 'src-v2/**'
      - 'scripts/migrate-v2.5.5.mjs'
      - 'scripts/validate-rain-guide-v2.5.5.mjs'
      - 'scripts/validate-data-schema.mjs'
      - 'scripts/build-v2.mjs'
      - 'scripts/validate-v2.mjs'
      - 'scripts/validate-v2.5-practical.mjs'
      - 'assets/v2.5.5/**'
      - 'src/v2.5.5.html'
      - 'index.html'
      - 'service-worker.js'
      - 'package.json'
      - '.github/workflows/validate-v2.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Apply v2.5.5 canonical migration
        run: node scripts/migrate-v2.5.5.mjs
      - name: Install deterministic build dependencies
        run: npm install --ignore-scripts
      - name: Validate rain safety data
        run: node scripts/validate-rain-guide-v2.5.5.mjs
      - name: Rebuild from canonical source
        run: |
          if ! node scripts/build-v2.mjs > /tmp/v255-build.log 2>&1; then
            echo '--- v2.5.5 build failure ---'
            tail -200 /tmp/v255-build.log
            exit 1
          fi
          tail -100 /tmp/v255-build.log
      - name: Upload build diagnostic
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: v2.5.5-build-error
          path: /tmp/v255-build.log
          if-no-files-found: error
      - name: Validate release chain and rain fallback
        run: |
          node scripts/validate-v2.mjs
          node scripts/validate-v2.5-practical.mjs
      - name: Upload preview site
        uses: actions/upload-artifact@v4
        with:
          name: travel-plans-v2.5.5-preview
          path: |
            index.html
            service-worker.js
            assets/v2.5.5
            assets/v2.5.4
            assets/v1.0.15
            versions/2026-08-08-v2.5.5.html
            versions/2026-07-31-v2.5.4.html
            versions/2026-07-27-v1.0.15.html
            DATA_SCHEMA_REPORT_v2.5.5.json
            MIGRATION_V2.5.5.json
            BUNDLE_BUDGET_v2.5.5.json
          if-no-files-found: error
