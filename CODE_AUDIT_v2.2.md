# 青岛旅行计划 v2.2.0 全面代码与功能审计

## 审计结论

v2.2.0 已完成本轮要求的核心改造，并继续保留 v1.0.15 作为不可变稳定回退。当前项目已从“单体页面加兼容补丁”进入“可持续拆分、自动迁移、双地图统一接口”的阶段，但尚未完全摆脱全局变量与 legacy 兼容层。

## 本轮已落实

### 1. 手机路线总览可进一步隐藏到侧边

路线抽屉现在具有 `hidden / collapsed / half / full` 四种状态：

- 第一次点击“收起”：进入底部收起状态。
- 在已收起状态再次点击“收起”：隐藏到右侧窄标签。
- 点击右侧“路线”标签：恢复底部收起状态。
- 上下拖动和键盘方向键同样支持四级切换。
- 状态保存在本机偏好中。

### 2. 静态数据已从 legacy-app.js 真正抽离

已独立维护：

- `src-v2/data/generated/points.js`
- `src-v2/data/generated/schedules.js`
- `src-v2/data/generated/hotels.js`
- `src-v2/data/generated/bookings.js`
- `src-v2/data/generated/sources.js`
- `src-v2/data/generated/recommendations.js`
- `src-v2/data/generated/catalog.js`

构建脚本会验证 `POINTS` 和 `SCHEDULES` 不再存在于 `legacy-app.js`。迁移报告保存在 `MIGRATION_V2.2.json`。

### 3. Leaflet 与高德地图采用共同适配接口

两套引擎实现已物理拆为：

- `src-v2/map/leaflet-adapter.js`
- `src-v2/map/amap-adapter.js`

由 `src-v2/map/map-adapters.js` 统一注册和调度。共同接口包括：

- `renderMarkers()`
- `renderRoute()`
- `setView()`
- `fitPoints()`
- `clearLayer()`
- `resize()`
- `view()`

现有 `rebuildMarkers`、`drawRoutes`、`fitPoints` 和 `clearRoutes` 已经过统一适配器入口执行并记录操作。

### 4. 出发前提醒中心

提醒中心使用北京时间，提供：

- 距离出发的阶段提醒。
- 酒店、租衣妆造、交通和行李复核提醒。
- 从 `BOOKINGS` 已记录规则中推导“提前 N 日开放”的预约窗口。
- 明确区分“预约开放窗口”和“固定截止日期”，不伪造售罄时间。
- 进入高德短期天气范围后的预报变化比较。
- 降雨、雷雨等天气提醒。
- 出发前最终检查。
- 当日隐藏单条提醒。

### 5. 离线、路线视野与服务诊断

- 增加“一键准备离线访问”，缓存当前应用壳、v2.2.0 载荷和固定 v1.0.15 入口。
- 不批量缓存地图瓦片，避免存储膨胀和地图服务授权问题。
- 高德路线规划新增“规划后自动调整地图视野”复选框。
- 高德服务面板增加调用次数、成功次数、失败次数和最近状态。

## 已查出问题及后续具体方案

### P1：legacy-app.js 仍约 84 KB

**问题：** 静态数据已抽离，但主要 UI 渲染、Leaflet 原始实现、高德原始实现和业务函数仍位于同一全局脚本中。

**方案：** 下一阶段按行为拆分：

- `ui/booking-panel.js`
- `ui/itinerary-panel.js`
- `ui/search-panel.js`
- `ui/hotel-panel.js`
- `map/marker-renderer.js`
- `map/route-renderer.js`
- `services/weather-service.js`
- `services/traffic-service.js`
- `services/search-service.js`

每拆出一组，增加等价行为测试后再删除对应 legacy 代码，避免一次性大重构。

### P1：地图适配器仍调用 legacy 渲染主体

**问题：** 统一接口与物理文件拆分已经完成，但部分真正的标注和路线绘制主体仍由 legacy 函数执行。

**方案：** 将两个引擎的 `renderMarkers`、`renderRoute`、`clearLayer` 实现逐步迁入各自 adapter；共享的标注模型、路线模型和 HTML 模板放入 `map/render-model.js`。完成后删除对全局函数的包装。

### P1：高德 Web API 密钥公开

**问题：** 当前仓库公开，Web API 密钥可被读取。

**方案：** 立即在高德控制台设置域名白名单、配额告警和异常调用监测；中期将 Web API 请求迁移到 Cloudflare Workers、Vercel Functions 等轻量代理。Web JS Key 仍按官方前端模式使用，但应限制域名。

### P2：服务诊断可能低估失败

**问题：** 部分旧函数内部捕获错误后以正常 Promise 结束，外层包装可能将其记为成功。

**方案：** 建立统一 `ServiceResult`：`{ok,data,error,source,cached,reportedAt}`，由天气、路况、搜索、路线和定位服务直接上报，而不是依靠函数包装推断。

### P2：提醒只在网页打开时运行

**问题：** 当前是页内提醒，关闭网页后不会主动推送。

**方案：** 增加 `.ics` 日历导出，将可确定的预约开放检查日、出发前 7/4/2 天任务加入用户日历。真正的后台推送需要 Web Push 后端或外部自动化，不在纯静态 GitHub Pages 中伪装实现。

### P2：天气能力存在上限

**问题：** 高德公开天气适合当前实况和短期日/夜预报，不提供可靠的逐小时降雨概率。远期日期不能提前给出真实天气。

**方案：** 保持“进入可用预报窗口后才提醒”的原则；若需要逐小时降雨概率，应接入具备授权和稳定 SLA 的天气服务，并在界面明确数据源、更新时间和预报时效。

### P2：离线不包含地图瓦片

**问题：** 无网络时行程文本可用，但地图底图可能不可用。

**方案：** 优先提供离线行程卡、地址、坐标、联系电话和高德导航链接；只有在地图服务条款允许时，才增加范围受限、用户主动触发、带容量估算的瓦片下载。

### P2：数据缺少正式 Schema

**问题：** 当前构建能检查文件和关键绑定，但尚未完整验证每个点位、日期、引用和坐标。

**方案：** 为 `POINTS / SCHEDULES / BOOKINGS / HOTELS / SOURCES` 增加 JSON Schema 或 Zod 校验：

- ID 唯一。
- 日期属于 2026-08-09 至 08-16。
- `pointIds` 必须引用真实点位。
- 经纬度范围合法。
- 每日路线序号连续。
- URL 协议和必填来源合法。

### P3：载荷继续增长

**问题：** v2.2.0 增加提醒、诊断和适配器后，单页载荷继续增长；目前仍会一次性加载所有面板代码。

**方案：** 在不破坏 GitHub Pages 单页发布的前提下，将地图助手、来源面板和酒店分析做延迟初始化；建立构建体积预算，超过阈值时 CI 报警。

### P3：本地数据缺少统一清理入口

**问题：** 页面会保存预约进度、底图、抽屉状态、天气快照和缓存，但尚无一个明确的“清除本机数据”按钮。

**方案：** 在设置中加入数据管理：查看本机保存内容、清除偏好、清除天气快照、清除离线缓存、全部重置，并在执行前列明影响范围。

## 功能拓展建议

1. **日历导出：** 将预约开放检查、酒店复核、出发前准备导出为 ICS。
2. **当天模式：** 旅行期间自动打开当天行程，显示“下一站、预计步行、实时路况、天气变化”。
3. **行程完成状态：** 每个站点支持完成、跳过、延后，并只在本机保存。
4. **雨天替代方案：** 降雨时自动把海边活动与室内备选进行对照，不自动修改正式行程，先给出建议。
5. **体力与热风险：** 根据连续户外时长、坡度、温度和午休间隔给出舒适度提示。
6. **交通费用预算：** 按已规划的打车段累计估算，不把估算值当成实际价格。
7. **分享与打印：** 生成某日简洁版路线卡，适合截图、打印或发给同行人。
8. **数据更新时间：** 每个天气、路况、预约规则和酒店数据明确显示来源时间与“实时/缓存/静态”状态。

## 测试与发布要求

v2.2.0 发布前必须通过：

- 源码、迁移数据、压缩载荷和入口回退检查。
- v1.0.15 固定历史入口检查。
- 1920×1080 与 2560×1440 Chromium。
- 安卓旗舰竖屏和横屏 Chromium。
- iPhone 旗舰竖屏和横屏 WebKit。
- 抽屉二次收起到侧边并恢复。
- 提醒中心和统一地图适配器存在性。
- 路况开关保持中心与缩放。
- 页面无横向溢出、主要浮层互斥。

## 稳定回退

无论后续如何迭代，以下内容不得由优化分支覆盖：

- `archive/v1.0.15-stable`
- `versions/2026-07-27-v1.0.15.html`
- `assets/v1.0.15/`

生产入口保持“当前版本优先，v1.0.15 最终回退”。
