# v2.5.6 PR validation scope

Draft validation only. Merge is blocked until v2.5.6/v3 build, 430×932 mobile interactions, desktop interactions, actual-route detail tests, rain-day pager tests, hourly precipitation tests, official-status assertions and frozen v2.5.4 rollback checks are green.
