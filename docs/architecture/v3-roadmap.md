# 青岛 v3 分阶段路线与门禁

| 阶段    | 交付物                                                                             | 当前状态       | 进入下一阶段的硬门禁                                                         | 回滚                                     |
| ------- | ---------------------------------------------------------------------------------- | -------------- | ---------------------------------------------------------------------------- | ---------------------------------------- |
| Phase 0 | 最新仓库审计、基线截图、README、功能矩阵、差距／风险、ADR                          | Draft PR 完成  | v2 构建、数据、架构、payload 与精确 HEAD E2E 有证据；Pages 未改              | revert 文档提交；v2 无变化               |
| Phase 1 | workspace、strict TS、Zod Schema、迁移、领域骨架、1 个点位纵向测试、只读 CI        | Draft PR 完成  | format、lint、typecheck、unit、property、migration 全通过；v2 门禁仍通过     | revert v3 代码提交；无运行时引用         |
| Phase 2 | 选择天数／点位／优先级→生成→地图→移动→重算→保存→导出导入                           | Draft PR 完成  | 单一最小闭环与 Planner 不变量、Web E2E 全通过                                | 保持 v2 正式入口；删除实验数据不影响用户 |
| Phase 3 | 完整编辑器、搜索加入、自定义点、多计划、批量、Logo、RouteStyle、撤销重做、住宿分析 | sidecar 已完成 | 单元、契约、迁移、Web E2E、桌面与手机截图通过；不把降级路线冒充真实 Provider | 停用 v3 sidecar；v2 正式入口不受影响     |
| Phase 4 | 青岛完整内容、研究批次、推广风险、动态观测、更新审核、预设                         | 未开始         | Schema 稳定；正式数据均 approved；可发布／回滚批次                           | 回滚到上一 content package               |
| Phase 5 | 小程序 PoC、技术 ADR、地图／存储 adapters、完整功能、真机矩阵                      | 未开始         | 100／300／500 点性能、存储迁移、分包、分享、功能一致性通过                   | 保留 Web 与 v2；不发布小程序包           |
| Phase 6 | 迁移演练、双端验收、正式发布和回滚                                                 | 未开始         | 原版 8 日完整恢复；主要功能一致；多设备 E2E；v2.5.4／v1.0.15 可回退          | 恢复 v2.5.4 Pages loader                 |

## Phase 3 完成项

1. 可执行 Undo／Redo 历史已经版本化，并与计划原子保存；页面重载可恢复。
2. 高德搜索通过运行时 SDK adapter 注入，不复制 Legacy 密钥；无 SDK 时明确降级到 49 个青岛点位。
3. 自定义地点、MarkerStyle、五种编号模式和 RouteStyle 已进入共享 Schema 与编辑器。
4. 多计划、快照、归档、软删除／恢复及批量编辑已经形成 IndexedDB 闭环。
5. 住宿区域只做带警告的直线距离初筛；正式推荐仍等待真实路线与经审核动态数据。

## 下一阶段

Phase 4 先稳定内容批次、来源等级、动态观测和审核发布模型，再扩大青岛内容数据。真实路线 Provider 网关需要独立 ADR 和凭据方案；它是正式迁移门禁，不允许通过复制 Legacy 前端密钥绕过。

每个切片独立提交；架构、内容数据、UI 和图标资源不混在同一提交。
