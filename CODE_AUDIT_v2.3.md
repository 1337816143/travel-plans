# 青岛旅行计划 v2.3.0 全面代码与功能审计

## 审计结论

v2.3.0 同时完成两类工作：

1. **行为等价的底层架构迁移**：拆分 `legacy-app.js`、建立共享渲染模型、把 Leaflet 与高德的真实绘制主体放入各自适配器。该部分不以改变功能、视觉或交互为目标。
2. **明确的新功能**：系统日历导出、正式数据 Schema、本机数据管理、当天模式、站点状态、雨天替代、体力与热风险、交通预算、简洁路线卡和数据状态标签。

高德 API 密钥、域名白名单、配额及服务端代理相关改造，按用户本轮要求明确延期，未在 v2.3.0 中修改。

生产入口继续保持：当前版本优先，v1.0.15 作为不可变最终回退。

---

## 一、P1 架构优化是否影响视觉和功能

### 结论

本轮 P1 架构工作采用“行为等价迁移”：

- 原函数主体按 AST 语法树边界迁出，函数内容不作视觉改写。
- 原函数名称和旧调用入口继续保留。
- 标注 HTML、路线标注 HTML、方向箭头 HTML 沿用 v2.2.0 的结构和 class。
- Leaflet 与高德继续使用相同点位、路线、颜色、图标和弹窗数据。
- 每次迁移均通过现有桌面、手机、横屏和路况视野回归测试。

因此，P1 本身不应造成可见功能变化。新增的“旅行工具”标签及其内容属于独立功能扩展，不是架构迁移的副作用。

---

## 二、`legacy-app.js` 缩减结果

v2.2.0 的 `legacy-app.js` 约为 84 KB。v2.3.0 自动迁移后约为 29 KB，减少约 65%。

已迁出的行为模块包括：

### UI

- `src-v2/ui/booking-panel.js`
- `src-v2/ui/itinerary-panel.js`
- `src-v2/ui/search-panel.js`
- `src-v2/ui/hotel-panel.js`
- `src-v2/ui/trip-operations.js`

### 地图

- `src-v2/map/render-model.js`
- `src-v2/map/marker-renderer.js`
- `src-v2/map/route-renderer.js`
- `src-v2/map/leaflet-adapter.js`
- `src-v2/map/amap-adapter.js`
- `src-v2/map/map-adapters.js`

### 服务

- `src-v2/services/weather-service.js`
- `src-v2/services/traffic-service.js`
- `src-v2/services/search-service.js`
- `src-v2/services/calendar-export.js`
- `src-v2/services/service-diagnostics.js`

### 状态

- `src-v2/state/local-data-manager.js`

迁移由 Acorn AST 解析器识别真正的顶层函数声明，避免字符串、模板字符串、正则表达式或默认参数造成范围误判。

### 仍留在 legacy 中的内容

- 全局基础状态和兼容变量。
- WGS84 / GCJ-02 坐标转换。
- 底图切换及高德加载入口。
- 页面启动、事件绑定和少量高德服务 UI 桥接。
- 旧版兼容所需的全局函数入口。

这些内容后续仍可继续拆分，但已不再包含预约面板、行程面板、酒店面板、搜索面板、天气、路况和主要地图渲染主体。

---

## 三、共享地图渲染模型

`src-v2/map/render-model.js` 现在统一提供：

- 普通点位标注 HTML。
- 路线序号标注 HTML。
- 方向箭头 HTML。
- 点位颜色、图标、短名称。
- 弹窗数据。
- 路线点和方向段模型。
- 统一图层名称。

统一图层名称包括：

- `tripMarkers`
- `tripRoutes`
- `hotels`
- `plannedRoute`
- `serviceSelection`
- `location`
- `traffic`

Leaflet 与高德适配器共同提供：

- `renderMarkers()`
- `renderRoute()`
- `setView()`
- `fitPoints()`
- `clearLayer()`
- `resize()`
- `view()`

### 当前实现状态

- Leaflet 标注、聚合、路线和方向标识的真实绘制主体已放入 `leaflet-adapter.js`。
- 高德标注、聚合、路线和方向标识的真实绘制主体已放入 `amap-adapter.js`。
- 旧全局入口通过 `map-adapters.js` 转发，用于保持兼容。

以后修改共享标注结构时，应优先修改 `render-model.js`，两套地图再分别消费相同模型。

---

## 四、正式数据 Schema

### Schema 是什么

Schema 可以理解为“数据体检规则”。它不改变景点或行程内容，而是在构建时检查数据是否自洽。

例如：

- 某天路线写了 `signal`，但点位表中没有 `signal`，构建应立即失败。
- 两个点位使用相同 ID，构建应立即失败。
- 纬度写成 360，构建应立即失败。
- 行程日期超出 8 月 9 日至 16 日，构建应立即失败。

这比等到网页打开后才发现路线缺点、地图跳错位置更可靠。

### v2.3.0 已检查

- 点位 ID 唯一。
- 经纬度处于合法范围。
- 日期属于 2026-08-09 至 08-16。
- `pointIds` 和每日路线引用的点位真实存在。
- 路线数组不存在序号空洞。
- URL 仅允许 HTTP / HTTPS。
- 点位必填来源不为空。

报告输出到：

- `DATA_SCHEMA_REPORT_v2.3.json`
- `src-v2/data/generated/schema-report.js`

Schema 报告同时在页面“旅行工具 → 数据状态”中展示。

---

## 五、ICS 系统日历导出

页面提供：

- 一键生成全部提醒。
- 每个提醒单独生成日历文件。

当前日历事件包括：

- 酒店订单复核。
- 租衣妆造订单复核。
- 出发前 7 天检查。
- 出发前 4 天天气检查。
- 出发前 2 天最终检查。
- 根据 `BOOKINGS` 中“提前 N 日 / 参观前 N 日”规则计算的预约开放检查日。

每个事件包含：

- 事件标题。
- 日期和北京时间 09:00。
- 说明。
- 分类。
- 10 分钟前提醒。
- 稳定 UID。

### 能力边界

网页下载的是标准 `.ics` 文件。用户仍需在 iOS、Android、Windows 或 macOS 中确认导入。纯静态 GitHub Pages 不能绕过系统授权直接写入日历。

---

## 六、本机数据管理

新增可单独清除：

- 预约进度。
- 天气快照。
- 已隐藏的出发提醒。
- 底图偏好。
- 抽屉状态。
- 站点状态与交通预算。
- 离线缓存。

并提供“全部恢复默认”。

所有操作只影响当前浏览器，不删除仓库文件，不修改 v1.0.15 固定版本。

---

## 七、功能拓展完成情况

### 1. 当天模式

- 按北京时间识别 2026-08-09 至 08-16。
- 旅行期间自动打开当天行程。
- 突出下一个未完成、未跳过的站点。
- 可以关闭自动打开偏好。

### 2. 站点状态

每个路线点支持：

- 未开始。
- 已完成。
- 跳过。
- 延后。

状态只保存在本机，并同步装饰逐日路线卡和简洁路线卡。

### 3. 雨天替代方案

- 读取高德短期天气中的降水现象。
- 展示当天正式 Plan B。
- 展示已有室内备选点。
- 不自动修改正式行程，不擅自删除海边项目。

### 4. 体力与热风险

根据以下信息给出低、中、高三级启发式提示：

- 短期最高温。
- 计划文本中可识别的户外时长。
- 坡路、登高和台阶。
- 是否安排午休或回酒店休息。

该结果不是医疗建议，不替代现场体感。

### 5. 交通费用预算

- 高德驾车路线返回打车费用估算时，可加入交通预算。
- 累计使用人民币显示。
- 明确标注为估算，不是叫车平台最终价格。
- 可以清空本机预算。

### 6. 每日简洁路线卡

支持：

- 复制纯文本。
- 下载独立 HTML。
- 打印。

内容包括日期、天气、路线、站点状态、摘要、预约和 Plan B。

### 7. 数据状态标签

当前展示：

- 行程与点位：静态版本。
- Schema：通过 / 需检查。
- 天气：尚未进入范围 / 短期预报。
- 路况：实时图层，明细缓存 30 秒。
- 高德服务：成功和失败次数。
- 酒店：地址静态，房价以订单页为准。

---

## 八、本轮主动查出并修复的问题

### 1. 自制函数扫描器无法可靠处理 JavaScript

**问题：** 嵌套模板字符串、正则表达式和默认对象参数会误导手写扫描器。

**修复：** 使用 Acorn AST 获取函数声明的准确 `start / end`，不再自行解析完整 JavaScript 语法。

### 2. 迁移函数后留下了提前执行的全局导出

**问题：** `window.launchAmap=launchAmap` 留在旧脚本中，但函数已迁到后续模块，导致旧脚本提前中断，天气常量未初始化。

**修复：** 将全局导出与 `launchAmap` 一起迁入 `marker-renderer.js`。

### 3. Schema 构建不应每次生成不同时间戳

**问题：** 动态生成时间会让同一源码产生不同载荷哈希。

**修复：** Schema 报告改为稳定的版本标识，保持可重复构建。

### 4. 天气清理范围与按钮文字不一致

**问题：** 原设计中“清除天气快照”也会清除已隐藏提醒。

**修复：** 拆成“天气快照”和“已隐藏的出发提醒”两个独立控制。

### 5. 服务诊断缺少稳定快照接口

**问题：** 页面数据状态无法可靠读取统计数据。

**修复：** 新增 `snapshot()`，统一返回调用、成功、失败和分服务统计。

---

## 九、仍存在的问题与具体优化方案

### P1：仍有约 29 KB legacy 兼容层

**问题：** 已大幅缩小，但启动、底图、坐标转换和部分高德 UI 绑定仍依赖全局变量。

**方案：** 下一阶段拆为：

- `core/app-state.js`
- `map/coordinate-service.js`
- `map/basemap-controller.js`
- `map/amap-loader.js`
- `ui/app-bootstrap.js`
- `ui/amap-assistant-controller.js`

每迁移一组，先增加等价测试，再删除全局入口。

### P1：适配器仍使用共享全局对象

**问题：** `map`、`amapInstance`、`routeLayer` 等仍由全局变量提供。

**方案：** 建立显式上下文：

```js
createLeafletAdapter({ map, clusters, routeLayer, hotelLayer, renderModel })
createAmapAdapter({ map: amapInstance, cluster, renderModel })
```

这样适配器可以独立测试，不依赖浏览器全局变量。

### P1：服务返回值尚未统一

**问题：** 某些旧服务内部捕获错误并正常结束，诊断统计可能仍低估失败。

**方案：** 天气、路况、搜索、定位和路线统一返回：

```js
{ ok, data, error, source, cached, reportedAt }
```

由服务本身报告成功或失败，不依靠外围 Promise 推断。

### P2：ICS 是导出，不是持续同步

**问题：** 行程或预约规则修改后，已导入系统日历的旧事件不会自动更新。

**方案：**

- 保持稳定 UID。
- 页面显示日历导出版本。
- 数据变化后提示“日历提醒已更新，请重新导入”。
- 后续可增加“取消旧事件”ICS。

### P2：重复点位共享一个状态

**问题：** 住宿点可能同时作为路线起点和终点，目前两次出现共享同一个点位状态。

**方案：** 状态键从 `date + pointId` 升级为 `date + routeOccurrence + pointId`，并提供旧数据迁移。

### P2：体力风险是文本启发式

**问题：** 当前不含真实坡度、逐小时体感温度和步行距离。

**方案：**

- 使用路线结果的步行距离和时长。
- 使用公开高程数据计算累计爬升。
- 接入具备授权的逐小时天气服务后计算热指数。
- 继续明确标注“提示，不是医疗结论”。

### P2：交通预算缺少手动调整

**问题：** 高德不一定返回费用，且价格会随平台、时段和供需变化。

**方案：** 支持：

- 手动新增预算段。
- 编辑估算值。
- 删除单段。
- 区分高德估算和用户实际支付。

### P2：雨天替代仅做固定对照

**问题：** 当前室内备选数量有限，没有根据距离、预约状态和开放时间排序。

**方案：** 为备选增加：

- 所在区域。
- 室内程度。
- 预约要求。
- 预计时长。
- 与当天主路线的绕行成本。

只给候选排序，不自动改正式行程。

### P2：本机数据需要版本迁移

**问题：** 后续修改状态结构时，旧 localStorage 可能不兼容。

**方案：** 为本机状态加入：

```js
{ schemaVersion, updatedAt, data }
```

并建立逐版本迁移函数。

### P3：载荷继续增长

**问题：** 新增工具后会增加 gzip 体积，所有工具仍一次加载。

**方案：**

- 旅行工具首次打开时再初始化。
- 日历、打印和数据管理延迟加载。
- CI 增加 gzip 体积预算和增幅阈值。

### P3：工具面板信息密度较高

**问题：** 旅行工具集中展示多个卡片，手机端滚动较长。

**方案：** 分成：

- 今日与行程。
- 提醒与日历。
- 风险与预算。
- 数据与设置。

或使用折叠分组，并记住展开状态。

---

## 十、可继续拓展的功能

1. **下一站模式**：显示当前位置到下一站的步行 / 地铁 / 打车建议。
2. **实际支出记录**：与预算分开，按日期和交通方式统计。
3. **同行人协作**：导出和导入本机状态文件，不依赖后端账户。
4. **预约状态日历联动**：预约标记完成后，生成或取消相应日历任务。
5. **行程变更日志**：记录本机对站点的完成、跳过、延后操作。
6. **雨天方案对比卡**：原计划与备选按距离、费用、室内程度并列比较。
7. **出发日仪表盘**：订单、天气、路况、下一站和未完成任务集中展示。
8. **可访问性增强**：高对比度模式、字号设置、完整键盘路线和屏幕阅读器说明。
9. **数据更新时间统一**：每项动态数据明确显示来源、查询时间、缓存年龄和失败状态。
10. **真实设备回归记录**：保留小米 HyperOS Chrome 与 iPhone Safari 的实机截图基线。

---

## 十一、明确延期事项

本轮不处理：

- 高德 Web API 密钥代理。
- 密钥轮换。
- 域名白名单和配额控制的自动配置。

原因：用户明确要求暂不进行该项优化。代码中不应把它标记为已完成。

仍建议用户在方便时自行进入高德控制台查看配额和异常调用，但这不是 v2.3.0 的发布阻断项。

---

## 十二、发布与回退要求

v2.3.0 合并前必须通过：

- AST 行为迁移和 legacy 体积检查。
- 正式数据 Schema。
- 源码、压缩载荷和入口一致性。
- v2.3.0 → v1.0.15 回退顺序。
- v1.0.15 固定历史入口。
- 1920×1080、2560×1440 Chromium。
- 安卓旗舰横竖屏 Chromium。
- iPhone 旗舰横竖屏 WebKit。
- 原有逐日路线、路线抽屉、侧边隐藏、路况视野保持。
- 新增旅行工具、ICS、本机数据管理和站点状态。
- 页面初始化无未捕获错误。

以下稳定内容不得被覆盖：

- `archive/v1.0.15-stable`
- `versions/2026-07-27-v1.0.15.html`
- `assets/v1.0.15/`
