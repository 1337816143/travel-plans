# 青岛旅行计划代码审计（v1.0.15）

## 本次已修复

1. **高德服务重复激活导致地图重新缩放**
   - 根因：`switchToAmap()` 每次调用都会执行 `renderAmapView()`，而后者会重新计算并适配全部点位视野。
   - 修复：当当前引擎已经是高德且实例存在时，`switchToAmap()` 直接返回现有实例，不重绘、不聚合、不适配视野。

2. **实时路况开关改变中心或缩放**
   - 修复：高德已激活时不再调用切换流程；切换前保存中心和缩放，操作后在下一动画帧恢复同一视野。

3. **不必要的详细路况请求**
   - 修复：只有地图助手处于打开状态时，首次进入高德底图才自动请求道路明细；实时路况瓦片图层本身仍按配置自动刷新。

## 审计结论

当前版本的主要功能链路已经完整：多底图、统一标注与聚合、每日路线、天气、搜索、定位、路线规划、路况、打车辅助和移动端适配均有明确实现。主要风险不再是“缺少功能”，而是工程结构、异步状态和长期维护成本。

## 优先级 P1：建议下一版本优先处理

### 1. 建立唯一的源代码入口

当前构建方式是从上一个版本的压缩载荷解码 HTML，再通过字符串定位替换函数和 DOM。优点是能够快速迭代，缺点是：

- 上一版本结构稍有变化就可能导致替换失败；
- 多个版本补丁叠加后难以定位真实来源；
- 正则、模板字符串和压缩载荷容易出现转义问题；
- 代码审查必须同时理解基础版本和多层补丁。

建议改为：

- `src/app.js`：应用状态与业务逻辑；
- `src/map/leaflet.js`、`src/map/amap.js`：地图适配器；
- `src/services/amap-api.js`：高德服务请求；
- `src/data/trip.js`：景点、日程和酒店数据；
- `src/styles/*.css`：界面样式；
- 使用 Vite 或 esbuild 输出最终静态 HTML/JS/CSS；
- 压缩载荷仅作为发布产物，不再作为下一版本的源代码。

### 2. 增加异步请求竞态控制

搜索联想、周边搜索、天气和路况请求可能并发。用户快速输入、拖动地图或切换半径时，较早发出的请求可能较晚返回，并覆盖最新结果。

建议：

- 为每类请求增加递增的 `requestId`；
- `fetch` 请求使用 `AbortController`；
- 高德插件回调在写入界面前检查请求是否仍为最新；
- 路况查询显示“数据中心位置”和请求时间，防止结果与当前地图中心不一致。

### 3. 统一地图视野状态

v1.0.15 已保证路况开关不改变视野，但从 OSM 切换到高德或从高德切回 OSM 时，仍应统一保存：

- 中心经纬度；
- 缩放级别；
- 当前选中日期；
- 当前弹窗或选中点；
- 路况图层状态。

建议建立 `MapViewState`，由两个地图引擎共同读写，切换底图时只转换坐标，不重新执行全量 `fitBounds`。

### 4. 建立覆盖物生命周期管理器

当前存在行程标注、行程路线、搜索选点、定位点和路线规划服务等多类覆盖物，分别保存在不同数组或插件实例中。长期迭代后容易发生旧覆盖物残留或误清理。

建议建立命名图层：

- `tripMarkers`
- `tripRoutes`
- `serviceSelection`
- `plannedRoute`
- `location`
- `traffic`

每类功能只操作自己的图层，切换日期时不影响用户刚规划的路线，清除路线时也不影响行程标注。

### 5. 加入浏览器级自动化与视觉回归

当前 CI 能验证载荷、JavaScript 语法和部分接口，但无法证明真实浏览器中的遮挡、点击和缩放行为。

建议使用 Playwright：

- 桌面 1920×1080、1366×768；
- iPhone 14/15、常见 Android 竖屏；
- 打开/关闭侧栏；
- 切换 OSM/高德；
- 切换路况前后断言中心和缩放不变；
- 打开地图助手、每日路线总览和天气详情；
- 保存截图并做视觉差异比较。

## 优先级 P2：性能和配额优化

### 6. 避免重复销毁 MarkerCluster

当前每次渲染高德行程视图会销毁并重建聚合实例。点位数量目前不大，影响有限；未来加入餐厅、酒店和推荐点后会增加开销。

建议优先调用聚合实例的数据更新方法，仅在配置变化时重建实例。

### 7. 细化路况刷新策略

代码中已经预留 `amapTrafficAutoTimer`，但详细道路态势尚未形成统一的周期刷新机制。

建议：

- 路况图层保持高德自身自动刷新；
- 详细道路列表仅在助手打开、页面可见时每 60–120 秒刷新；
- `document.visibilityState !== 'visible'` 时暂停；
- 地图移动后防抖刷新；
- 同一中心和半径短时间内复用结果。

### 8. 天气局部更新而不是重建全部日程 DOM

天气更新目前会重新执行 `renderDays()` 和 `renderLegend()`。虽然保留了展开状态，但可能影响键盘焦点、滚动位置，并产生不必要的 DOM 重建。

建议为天气元素设置 `data-weather-date`，只更新对应文本和详情。

### 9. 节流移动端 visualViewport 事件

移动端浏览器地址栏变化会高频触发 `visualViewport.resize/scroll`。当前每次事件都会立即写入 CSS 变量。

建议用 `requestAnimationFrame` 合并同一帧内的多次更新，降低布局抖动。

## 优先级 P3：安全、可用性和产品化

### 10. API 凭证与额度保护

当前为公开 GitHub Pages，前端凭证必然可以被访问者查看。建议：

- 高德控制台限制允许域名；
- 为 JS API 和 Web API 分别设置最小权限；
- 设置额度告警和异常调用监控；
- 高消耗 Web API 逐步迁移到轻量服务端代理；
- 定期轮换密钥。

### 11. 可访问性

建议补充：

- 地图标注的键盘焦点与可读标签；
- 弹窗打开后的焦点管理；
- 路况颜色同时显示文字或图案，避免只依赖颜色；
- 所有动态状态使用适当的 `aria-live`。

### 12. 离线与弱网体验

建议增加 Service Worker：

- 缓存应用壳、行程数据和样式；
- 地图服务不可用时仍可查看完整日程、酒店和预约清单；
- 明确提示天气、路况和路线为在线功能；
- 不大规模缓存地图瓦片，避免授权和存储问题。

## 推荐实施顺序

1. Playwright 实机行为测试和地图视野状态模型；
2. 异步请求竞态控制；
3. 覆盖物生命周期管理；
4. 代码模块化和正式构建系统；
5. 性能、配额、离线和可访问性优化。
